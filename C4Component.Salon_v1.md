```mermaid
C4Component
title Braid Technologies Studio - C4 Component Diagram

// External Systems
System_Ext(API_System, "API System", "A system providing API functionalities")
System_Ext(OpenAI, "OpenAI", "Third-party AI model service")
System_Ext(Azure, "Azure", "Cloud services")

// Person
Person(Developer, "Developer", "A software developer")

// System Boundaries
System_Boundary(Braid_Studio, "Braid Technologies Studio") {
    
    Container_Boundary(FrontEnd, "Front End") {
        Container(Cascade, "Cascade", "Edge Plugin", "Uses JavaScript/TypeScript to scrape and classify web page text")
        Container(Boxer, "Boxer", "Web Frontend", "AI-enabled learning assistant with database integration and LLM support")
        Container(Teams, "Teams Integration", "Teams Plugin", "Brings Boxer & Waterfall into Teams environment")
    }

    Container_Boundary(BackEnd, "Back End") {
        Container(Waterfall, "Waterfall", "Data Analysis Pipeline", "Python-based summarization and cluster analysis")
        Container(Api, "API", "Azure Functions", "Handles calls to Azure hosted OpenAI model")
        Container(ApiTest, "ApiTest", "Testing Framework", "Generated by Salon to test APIs from Python classes")
        Container(CommonTs, "CommonTs", "Utility Library", "Typescript utility classes for API definitions and type definitions")
        Container(CommonPy, "CommonPy", "Utility Library", "Python utility classes to access TypeScript server APIs")
        Container(Salon, "Salon", "Engineering Helper", "Scripts to assist in code generation and repository text processing")
    }

    Container_Boundary(Resources, "Resources and Services") {
        Container(AzureFunctions, "Azure Functions", "Serverless Functions", "Handles business logic and data operations")
        Container(CosmosDB, "Azure Cosmos DB", "Database", "Stores data")
    }
}

// Relationships
Rel(Developer, Cascade, "Uses Edge plugin")
Rel(Developer, Boxer, "Uses the AI-enabled learning assistant")
Rel(Developer, Teams, "Uses Teams plugin")
Rel(Cascade, Api, "Calls Braid API")
Rel(Waterfall, Api, "Calls Braid API")
Rel(Boxer, Api, "Calls Braid API")
Rel(Api, OpenAI, "Interacts with for AI model tasks")
Rel(Api, AzureFunctions, "Calls Azure Functions for operations")
Rel(Api, CosmosDB, "Stores and retrieves data from")
Rel(ApiTest, Api, "Tests API endpoints")
Rel(CommonTs, Api, "Provides utility classes for TypeScript")
Rel(CommonPy, Api, "Provides utility classes for Python")
Rel(Salon, ApiTest, "Generates test code for APIs")
Rel(Salon, RepoText, "Summarizes and generates text from repositories")

```

%% Generated by Salon from Braid Technologies, 09/03/2025
