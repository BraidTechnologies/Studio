```mermaid
C4Context
  Person(user, "User", "Interacts with Braid applications")

  System_Boundary(c1, "Braid Technologies Studio") {
    System(waterfall, "Waterfall", "Data analysis backend pipeline (Python)")
    System(cascade, "Cascade", "Edge plugin for text analysis (Typescript)")
    System(boxer, "Boxer", "AI-enabled learning assistant (Web frontend)")
    System(teams, "Teams Plugin", "Integrates Boxer & Waterfall into Teams")
    System(api, "Braid APIs", "Azure Functions for AI models and database access")
    System(salon, "Salon", "Scripting tools for code generation and testing")

    Rel(cascade, api, "Uses", "API calls")
    Rel(waterfall, api, "Uses", "API calls")
    Rel(boxer, api, "Uses", "API calls")
    Rel(teams, boxer, "Uses", "")
    Rel(teams, waterfall, "Uses", "")
    Rel(salon, api, "Tests", "Generates ApiTest code")

    System_Boundary(c2, "Testing Tools") {
      System(apitest, "ApiTest", "Generated Python code for API testing")
      System(boxereval, "BoxerEval", "Evaluation tools for Boxer")
    }
    Rel(salon, apitest, "Generates", "")
    Rel(boxer, boxereval, "Evaluated by", "")


    System_Boundary(c3, "Shared Components") {
      System(commonts, "CommonTs", "Typescript utility classes")
      System(commonpy, "CommonPy", "Python utility classes")
    }
    Rel(cascade, commonts, "Uses", "")
    Rel(api, commonts, "Uses", "")
    Rel(waterfall, commonpy, "Uses", "")
    Rel(apitest, commonpy, "Uses", "")
  }

  Rel(user, cascade, "Uses", "Web browser")
  Rel(user, boxer, "Interacts with", "Web browser")
  Rel(user, teams, "Uses", "Microsoft Teams")


  System_Ext(azure, "Azure Cloud", "Hosting and services")
  System_Ext(openai, "OpenAI", "AI models")

  Rel(api, azure, "Deployed on", "")
  Rel(api, openai, "Uses", "API calls")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
