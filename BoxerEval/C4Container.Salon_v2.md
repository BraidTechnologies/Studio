```mermaid
C4Container

System_Boundary(c1, "BoxerEval") {
    Container(boxer_data_test, "BoxerDataTest (v5)", "Python", "Generates enriched questions, calculates similarity embeddings, evaluates quality, and manages test scenarios. Entry point for testing.")
    Container(gemini_evaluator, "GeminiEvaluator", "Python", "Evaluates generated content quality using Google's Gemini LLM.")
    Container(persona_strategy, "PersonaStrategy", "Python", "Implements different professional personas (Developer, Tester, Business Analyst) for question generation.")
    Container(test_runner, "TestRunner", "Python", "CLI interface for running different test scenarios.")
    Container(api_configuration, "ApiConfiguration", "Python", "Manages API configurations for Azure OpenAI and Google Gemini.")
    Container(common_functions, "common_functions", "Python", "Provides utility functions like directory creation, embedding generation.")

    Rel(boxer_data_test, gemini_evaluator, "Uses", "API")
    Rel(boxer_data_test, persona_strategy, "Uses", "")
    Rel(boxer_data_test, api_configuration, "Uses", "")
    Rel(boxer_data_test, common_functions, "Uses", "")
    Rel(test_runner, boxer_data_test, "Uses", "")
    Rel(test_runner, persona_strategy, "Uses", "")

}

System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models for question generation and embedding calculation.")
System_Ext(google_gemini, "Google Gemini", "Provides LLM for quality evaluation.")

Rel(boxer_data_test, azure_openai, "Uses", "API")
Rel(gemini_evaluator, google_gemini, "Uses", "API")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
