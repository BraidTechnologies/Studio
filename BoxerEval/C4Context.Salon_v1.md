```mermaid
C4Context
title BoxerEval System Context Diagram

Person(admin, "Administrator", "Manages configurations and oversees the testing framework")
Person(user, "User", "Interacts with the testing framework for generating and evaluating AI-assisted questions")

System_Boundary(boxerEval, "BoxerEval System") {
    System(boxerEvalFramework, "BoxerEval Framework", "A testing framework for evaluating and generating AI-assisted questions using Azure OpenAI services")

    Container(coreModules, "Core Testing Modules", "Processes questions through similarity embedding and generates enriched questions using the Azure OpenAI API") {
        Container(boxerDataTestV1, "BoxerDataTest_v1.py", "Processes and evaluates questions")
        Container(boxerDataTestV2, "BoxerDataTest_v2.py", "Facilitates question generation and similarity analysis")
        Container(boxerDataTestV3, "BoxerDataTest_v3.py", "Handles persona-based question generation and evaluation")
        Container(boxerDataTestV4, "BoxerDataTest_v4.py", "Generates enriched and follow-up questions")
        Container(boxerDataTestV5, "BoxerDataTest_v5.py", "Enhanced features like follow-up question generation and improved evaluation metrics")
    }

    Container(supportingComponents, "Supporting Components", "Provides additional functionalities and evaluation mechanisms") {
        Container(geminiEvaluator, "GeminiEvaluator.py", "Evaluates generated content quality using Google's Gemini LLM")
        Container(personaStrategy, "PersonaStrategy.py", "Implements different professional personas for question generation")
        Container(testRunner, "TestRunner.py", "CLI interface for running different test scenarios")
    }

    Container(apiConfiguration, "ApiConfiguration.py", "Configures API settings for Azure OpenAI and Google's Gemini API")
    Container(commonFunctions, "common_functions.py", "Provides utility functions for directory creation, embedding generation, and API configuration handling")
}

System_Ext(azureOpenAI, "Azure OpenAI", "Provides GPT models and embedding services")
System_Ext(googleGemini, "Google Gemini", "Provides content quality evaluation services")

Rel(admin, boxerEvalFramework, "Manages and configures the framework")
Rel(user, boxerEvalFramework, "Generates and evaluates questions")
Rel(boxerEvalFramework, azureOpenAI, "Uses", "API calls for generating questions and embeddings")
Rel(boxerEvalFramework, googleGemini, "Uses", "API calls for quality evaluations")

Rel(coreModules, supportingComponents, "Interacts with")
Rel(apiConfiguration, supportingComponents, "Configures")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
