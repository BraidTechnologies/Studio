C4Context
title BoxerEval: AI-Assisted Question Generation and Evaluation Framework

Person(user, "User", "Runs tests and analyzes results")

System(boxereval, "BoxerEval", "Testing framework for evaluating and generating AI-assisted questions")

System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models for question generation and embedding calculation")

System_Ext(google_gemini, "Google Gemini", "Evaluates generated content quality")

Rel(user, boxereval, "Runs tests using", "CLI (TestRunner)")

Rel(boxereval, azure_openai, "Uses", "Azure OpenAI API")

Rel(boxereval, google_gemini, "Uses", "Gemini API")


System_Boundary(c1, "BoxerEval Components") {
  Container(core_testing, "Core Testing Modules (v1-v5)", "Python modules (BoxerDataTest_v1.py - v5.py) implementing core testing logic, including question generation, similarity calculation, and evaluation")

  Container(gemini_evaluator, "GeminiEvaluator", "Python module (GeminiEvaluator.py) for evaluating content quality using Google Gemini")

  Container(persona_strategy, "PersonaStrategy", "Python module (PersonaStrategy.py) implementing different professional personas for question generation")

  Container(test_runner, "TestRunner", "Python module (TestRunner.py) providing CLI for running test scenarios")

  Container(api_configuration, "ApiConfiguration", "Python module (ApiConfiguration.py) managing API settings")
    
  Container(common_functions, "Common Functions", "Python module (common_functions.py) containing utility functions such as directory creation and embedding generation")

}

Rel(user, test_runner, "Interacts with", "CLI commands")

Rel(test_runner, core_testing, "Executes", "Function calls")

Rel(core_testing, persona_strategy, "Uses", "Class instantiation")

Rel(core_testing, gemini_evaluator, "Uses", "Class instantiation")

Rel(core_testing, azure_openai, "Interacts with", "API calls")

Rel(gemini_evaluator, google_gemini, "Interacts with", "API calls")

Rel(core_testing, common_functions, "Uses", "Function calls")

Rel(common_functions, azure_openai, "Interacts with", "API calls for embeddings")

Rel(api_configuration, core_testing, "Provides configuration to")
Rel(api_configuration, common_functions, "Provides configuration to")
Rel(api_configuration, gemini_evaluator, "Provides configuration to")

%% Generated by Salon from Braid Technologies, 28/02/2025
