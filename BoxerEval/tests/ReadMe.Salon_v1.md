**BoxerDataTest_v1.py**

This Python code evaluates the relevance of test questions against a pre-existing knowledge base. It uses Azure OpenAI's `text-embedding-ada-002` model to generate embeddings for questions and calculate cosine similarity to pre-processed question chunks stored in JSON files.  The code includes retry logic for API calls and error handling.  `run_tests` orchestrates the process: configuring the OpenAI client, reading pre-processed data, processing questions, and saving results with timestamps to a specified directory.  A similarity threshold determines relevance, and the best match's summary and relevance score are recorded for each question.


**BoxerDataTest_v2.py**

This code implements a system for generating and evaluating questions related to AI application development. It uses Azure OpenAI for question generation, enrichment, and similarity embedding calculations.  The system reads pre-processed question chunks from JSON files, generates new questions (or uses provided ones), enriches them using a specified persona (like developer or tester), and then calculates the cosine similarity between the enriched questions and the existing chunks. Results, including hit relevance and summaries, are saved to a JSON file.  The code includes retry logic for OpenAI API calls and error handling.  It supports different persona strategies for generating questions tailored to different roles.


**BoxerDataTest_v3.py**

This code evaluates the performance of a question generation and enrichment system using OpenAI.  It reads a set of pre-processed question chunks with embeddings from a source directory. It then generates new questions, either provided directly or via a persona strategy (developer, tester, or business analyst).  The code enriches these questions using OpenAI, calculates embeddings for the enriched questions, and compares them to the existing chunks using cosine similarity. It also generates follow-up questions based on the most similar chunk and assesses whether these follow-up questions are on-topic. Finally, the code saves the results, including the original question, enriched question, hit status, similarity score, follow-up question, and on-topic assessment, to a JSON file.  Error handling and retry logic are implemented for robustness.


**BoxerDataTest_v4.py**

This code evaluates the performance of a question-answering system using GPT-4, text embeddings, and Gemini.  It loads pre-processed question chunks with embeddings from a source directory. It then generates enriched questions based on provided or persona-generated questions using GPT-4. It calculates the cosine similarity between the embeddings of generated questions and pre-processed chunks to determine relevance.  A follow-up question is generated for relevant hits, and its topic relevance is assessed. Finally, Gemini evaluates the quality of GPT-4's enriched question summaries against the original questions, and all results are saved to a JSON file.  The code includes retry logic for API calls and handles various error conditions.


**BoxerDataTest_v5.py**

This code evaluates the performance of a question-answering system. It uses `gpt-4o` to generate questions based on different personas (developer, tester, business analyst).  It then enriches these questions using another `gpt-4o` prompt.  The code calculates the similarity between these enriched questions and a set of pre-calculated question embeddings using `text-embedding-3-large`.  If a question's embedding is similar enough to an existing one, it's considered a hit.  A follow-up question is then generated using `gpt-4o` for each hit, and the code checks if this follow-up question is on-topic (related to AI). Finally, `gemini-1.5-pro` evaluates the quality of the enriched questions generated by `gpt-4o`.  Results are saved to a JSON file.  The code incorporates retry logic for OpenAI API calls and handles various error scenarios.


**GeminiEvaluator.py**

This code defines a `GeminiEvaluator` class that uses Google's Gemini model to assess the quality of text summaries.  It takes the original content and a generated summary as input, and returns a score between 1 (poor) and 4 (excellent). The evaluator uses the `google.generativeai` library and requires a `GEMINI_API_KEY` environment variable for authentication.  The `evaluate` method sends the content and summary to the Gemini model with specific instructions to judge summary quality, returning the model's numerical rating.


**PersonaStrategy.py**

This code implements a Strategy pattern to generate persona-specific questions about LLMs using Azure OpenAI.  An abstract base class `PersonaStrategy` defines the question generation interface.  Concrete strategies like `DeveloperPersonaStrategy`, `TesterPersonaStrategy`, and `BusinessAnalystPersonaStrategy` implement this interface, each using a tailored prompt to generate questions relevant to their respective persona.  The `_generate_questions` helper method handles the interaction with the OpenAI API, taking a prompt and number of questions as input.  The code also includes logging and error handling.


**run_BoxerDataTest.py**

This script runs a suite of tests on Large Language Models (LLMs) using the Boxer testing framework.  It defines a list of LLM-related questions and uses either `BoxerDataTest_v1` or `BoxerDataTest_v2` (configured by uncommenting the relevant import) to execute the tests.  The script configures API settings, input/output directories, and logging.  It creates a test output directory if one doesn't exist and then calls the `run_tests` function with the configuration, directories, and questions.  Error handling is implemented to catch and log issues during directory creation and test execution.


**TestRunner.py**

This script, `TestRunner.py`, provides a command-line interface for testing LLM-based systems.  It offers two main testing modes: static question testing and persona-based testing (Developer, Tester, Business Analyst).  The script configures Azure OpenAI clients, manages test execution and output directories, and handles logging and errors.  Users select a testing mode, and the script runs predefined questions or persona-specific tests against an LLM, saving results to a designated directory.  It leverages several modules, including `BoxerDataTest_v5` for core testing logic and `PersonaStrategy` for persona-based testing.


Generated by Salon from Braid Technologies, 28/02/2025