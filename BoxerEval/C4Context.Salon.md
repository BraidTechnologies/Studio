```mermaid
C4Context
title BoxerEval: AI-Assisted Question Generation and Evaluation Framework

Person(user, "User", "Runs tests and analyzes results")

System(boxereval, "BoxerEval", "Testing framework for evaluating and generating AI-assisted questions")

Rel(user, boxereval, "Runs tests", "CLI")

System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models for question generation and embedding calculation")
Rel(boxereval, azure_openai, "Uses", "API")

System_Ext(google_gemini, "Google Gemini", "Provides quality evaluation of generated content")
Rel(boxereval, google_gemini, "Evaluates quality with", "API")

System_Boundary(c1, "BoxerEval Components") {
  Container(core_modules, "Core Testing Modules (v1-v5)", "Python modules (BoxerDataTest_v1.py - v5.py) containing core testing logic")

  Container(gemini_evaluator, "GeminiEvaluator.py", "Evaluates generated content quality using Google's Gemini LLM")
  Rel(core_modules, gemini_evaluator, "Uses", "")

  Container(persona_strategy, "PersonaStrategy.py", "Implements different professional personas for question generation")
  Rel(core_modules, persona_strategy, "Uses", "")
    
  Container(test_runner, "TestRunner.py", "CLI interface for running different test scenarios")
  Rel(user, test_runner, "Interacts with", "CLI")
  Rel(test_runner, core_modules, "Uses", "")
  
  Container(api_configuration, "ApiConfiguration.py", "Manages API keys, endpoints, and settings")
  Rel(core_modules, api_configuration, "Uses", "")

  Container(common_functions, "common_functions.py", "Provides utility functions (directory creation, embedding generation)")
  Rel(core_modules, common_functions, "Uses", "")
}
```

%% Generated by Salon from Braid Technologies, 28/02/2025
