C4Context
  title BoxerEval - AI-Assisted Question Testing Framework

  Person(user, "User", "Runs tests and analyzes results")

  System_Boundary(c1, "BoxerEval") {
    Container(testrunner, "TestRunner", "Python", "CLI interface for running tests", "Python, Click")
    Container(boxertest, "BoxerDataTest_v5", "Python", "Core testing logic (question generation, embedding calculation, evaluation)", "Python, Azure OpenAI SDK, Tenacity, NumPy")
    Container(geminievaluator, "GeminiEvaluator", "Python", "Evaluates content quality using Google Gemini", "Python, Google Gemini API Client")
    Container(personastrategy, "PersonaStrategy", "Python", "Implements different professional personas for question generation", "Python")
    Container(apiconfiguration, "ApiConfiguration", "Python", "Manages API configurations (Azure OpenAI, Google Gemini)", "Python")
    Container(commonfunctions, "common_functions", "Python", "Utility functions (directory creation, embedding generation)", "Python, Azure OpenAI SDK")
    Rel(testrunner, boxertest, "Uses", "Python API calls")
    Rel(testrunner, personastrategy, "Uses", "Python API calls")
    Rel(boxertest, geminievaluator, "Uses", "Python API calls")
    Rel(boxertest, apiconfiguration, "Uses", "Reads configuration")
    Rel(boxertest, commonfunctions, "Uses", "Calls utility functions")
    Rel(geminievaluator, apiconfiguration, "Uses", "Reads configuration")
    Rel(personastrategy, apiconfiguration, "Uses", "Reads configuration")
    Rel(commonfunctions, apiconfiguration, "Uses", "Reads configuration")
  }


  System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models and embedding services")
  System_Ext(google_gemini, "Google Gemini", "Provides LLM for quality evaluation")
  System_Ext(git_repo, "Git Repository", "Stores code and test data")

  Rel(user, testrunner, "Runs", "CLI commands")
  Rel(boxertest, azure_openai, "Uses", "API calls")
  Rel(geminievaluator, google_gemini, "Uses", "API calls")
  Rel(testrunner, git_repo, "Clones from", "Git clone")

%% Generated by Salon from Braid Technologies, 28/02/2025
