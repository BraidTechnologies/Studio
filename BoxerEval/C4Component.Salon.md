```mermaid
C4Component
title BoxerEval Component Diagram

System_Boundary(c1, "BoxerEval") {
    Component(boxer_data_test, "BoxerDataTest_v5", "Generates enriched questions, calculates similarity, handles follow-up questions and evaluation", "Python")
    Component(gemini_evaluator, "GeminiEvaluator", "Evaluates generated content quality using Google's Gemini LLM", "Python")
    Component(persona_strategy, "PersonaStrategy", "Implements different professional personas for question generation", "Python")
    Component(test_runner, "TestRunner", "CLI interface for running different test scenarios", "Python")
    Component(api_configuration, "ApiConfiguration", "Manages API settings (Azure OpenAI, Google Gemini)", "Python")
    Component(common_functions, "common_functions", "Utility functions (directory creation, embedding generation)", "Python")
}

System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models for text generation and embeddings")
System_Ext(google_gemini, "Google Gemini", "Provides LLM for quality evaluation")

Rel(boxer_data_test, azure_openai, "Uses", "API calls for text generation and embeddings")
Rel(boxer_data_test, gemini_evaluator, "Uses", "Quality evaluation")
Rel(boxer_data_test, persona_strategy, "Uses", "Persona-based question generation")
Rel(gemini_evaluator, google_gemini, "Uses", "API calls for quality evaluation")
Rel(test_runner, boxer_data_test, "Uses", "Executes tests")
Rel(test_runner, persona_strategy, "Uses", "Selects persona strategy")
Rel(boxer_data_test, api_configuration, "Uses", "API settings")
Rel(gemini_evaluator, api_configuration, "Uses", "API settings")
Rel(common_functions, api_configuration, "Uses", "API Configuration")
Rel(boxer_data_test, common_functions, "Uses", "Utility Functions")


```

%% Generated by Salon from Braid Technologies, 28/02/2025
