```mermaid
C4Context
title BoxerEval: AI-Assisted Question Generation and Evaluation

Person(user, "User", "Runs tests and analyzes results")

System_Boundary(c1, "BoxerEval") {
    System(boxereval, "BoxerEval", "Testing framework for AI-assisted question generation and evaluation")

    Container(core_testing, "Core Testing Modules (v1-v5)", "Progressive versions of the testing framework, including enhanced features and improved evaluation metrics", "Python")
    Container(gemini_evaluator, "GeminiEvaluator", "Evaluates generated content quality using Google's Gemini LLM", "Python")
    Container(persona_strategy, "PersonaStrategy", "Implements different professional personas for question generation", "Python")
    Container(test_runner, "TestRunner", "CLI interface for running different test scenarios", "Python")
    Container(api_configuration, "ApiConfiguration", "Manages API settings for Azure OpenAI and Google Gemini", "Python")
    Container(common_functions, "common_functions", "Provides utility functions for directory creation, embedding generation, and API configuration handling", "Python")


    Rel(core_testing, gemini_evaluator, "Uses", "API")
    Rel(core_testing, persona_strategy, "Uses", "")
    Rel(core_testing, api_configuration, "Uses", "Configuration")
    Rel(core_testing, common_functions, "Uses", "Utility Functions")
    Rel(test_runner, core_testing, "Runs", "")
    Rel(test_runner, persona_strategy, "Uses", "")
    Rel(test_runner, api_configuration, "Uses", "Configuration")
    Rel(gemini_evaluator, api_configuration, "Uses", "Configuration")


}

System_Ext(azure_openai, "Azure OpenAI", "Provides GPT models for question generation and embedding calculation")
System_Ext(google_gemini, "Google Gemini", "Provides LLM for quality evaluation")

Rel(user, test_runner, "Interacts with", "CLI")
Rel(boxereval, azure_openai, "Uses", "API")
Rel(boxereval, google_gemini, "Uses", "API")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
