**Directory tests**

This codebase evolves through several versions (`BoxerDataTest_v1` to `v5`) to build a robust question generation and evaluation system using Azure OpenAI.  Early versions focus on basic question enrichment and similarity calculations using embeddings.  Later versions introduce persona-based question generation (using `PersonaStrategy`), follow-up question assessment (`GeminiEvaluator`), and more sophisticated evaluation metrics.  `TestRunner` orchestrates test execution, offering both static and persona-based testing options.  Helper modules manage API configuration (`ApiConfiguration`), retry logic (`tenacity`), and result storage (`TestResult`).  The code emphasizes modularity, error handling, and logging throughout its development.


Generated by Salon from Braid Technologies, 27/02/2025