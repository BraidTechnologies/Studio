**Directory tests**

This codebase evolves through several versions (`BoxerDataTest_v1` to `v5`) to build a robust question generation and evaluation system using Azure OpenAI.  Early versions focus on basic question enrichment and similarity analysis. Later versions incorporate persona-based question generation (using `PersonaStrategy`), follow-up question assessment (`GeminiEvaluator`), and more sophisticated evaluation metrics.  `TestRunner` orchestrates test execution, offering different testing strategies (static vs. persona-based) and managing configurations (`ApiConfiguration`). Supporting modules like `GeminiEvaluator` and `PersonaStrategy` handle specialized tasks, enhancing the system's capabilities for evaluating LLM-generated text and creating context-specific questions.  Throughout the development, retry logic, logging, and consistent data structures (`TestResult`) ensure robustness and maintainability.


Generated by Salon from Braid Technologies, 28/02/2025