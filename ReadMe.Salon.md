**Directory Api**

This codebase defines serverless functions for text processing, leveraging AI models for tasks like chunking, classification, and summarization.  It uses Azure Cosmos DB for storage and implements session-based authentication for data access.  Integration with Microsoft Teams and LinkedIn facilitates model discovery and repository management.  Utility functions provide standardized error handling and session validation.  Deployable as Azure Functions or runnable locally, the code emphasizes reliability through robust error handling and retry mechanisms.


**Directory ApiTest**

This project uses `pytest` and `requests` to test different API endpoints. Each test module focuses on a specific endpoint (e.g., `/chunk`, `/classify`).  Tests ensure correct responses for valid requests and proper error handling for invalid ones.  `jsonschema` validates response schemas where needed.  Tests utilize environment variables for base URL and authentication, construct full endpoint URLs, and check HTTP status codes (200 for OK, 400 for bad request).  Fixtures provide test data.  Test scenarios range from missing parameters to specific functionalities like surveys and code summarization.


**Directory BoxerEval**

This code iteratively develops a question generation and evaluation system using Azure OpenAI.  Starting with basic question enrichment and similarity checks, it progresses to incorporate persona-based question generation (`PersonaStrategy`) and advanced evaluation using `GeminiEvaluator`.  `TestRunner` manages test execution with configurable strategies and uses `ApiConfiguration` for settings.  `TestResult` provides a consistent data structure.  Retry mechanisms, logging, and specialized modules enhance robustness and maintainability throughout versions `BoxerDataTest_v1` to `v5`.


**Directory CommonPy**

This code interacts with a Braid Cosmos database to manage data chunks and pages.  `chunk_repository_api` and `page_repository_api` handle chunk/page operations (save, load, manage) using HTTP requests. Data structures like `IStoredChunk` and `IStoredPage` define the data format, inheriting from a base interface `IStorable`.  Utility functions help with type conversion and file operations within each respective API. The code relies on environment variables for configuration and logging for tracking events.


**Directory CommonTs**

This project manages interactions with Braid's AI services and ensures consistent prompt ID usage. The `scripts` directory automates the generation of TypeScript and Python files defining prompt ID constants from JSON data. The `src` directory contains APIs for interacting with Braid's services, including managing activities, text chunks, pages, models, and AI personas.  It also handles authentication, environment configuration, and Fluid Framework integration. The `test` directory includes tests using Mocha and Expect, covering chat model responses, text splitting, embedding generation, and prompt template management.


**Directory Salon**

This project automates code analysis and documentation using Python.  `src/api_to_test_code.py` generates tests from API specs, while `src/repo_to_c4.py` creates C4 diagrams and `src/repo_to_text.py` summarizes code using external APIs and NLTK.  Tests in the `test` directory cover these functionalities, including directory traversal (`test_directory_walker`), model interactions (`test_model_drivers`), and end-to-end script execution (`test_repo_to_c4`, `test_repo_to_text`).  The tests utilize `pytest` and `unittest`, employing mocks for isolation and featuring integration tests (often skipped).


**Directory WaterfallBrowser**

Waterfall Browser is a React app displaying hierarchical data.  `App.tsx` lays out the UI and uses `ChunkRetriever.tsx` to fetch data from a URL parameter.  `ChunkRetriever` manages the display of loading, error, or the data view (`ChunkView`) itself.  `ChunkView` presents the data and links to related data chunks. The app uses Fluent UI and custom styling.  `Defusc.tsx` decodes an API key for data retrieval, while `reportWebVitals.ts` tracks performance, and `UIString.ts` manages UI text.


Generated by Salon from Braid Technologies, 28/02/2025