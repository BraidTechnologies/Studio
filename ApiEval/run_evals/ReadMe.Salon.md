**run_evals.py**

The code begins by importing several modules required for its functionality, including `os`, `sys`, `pytest`, `json`, `datetime`, and `pathlib.Path.`

The project root directory is added to the Python path to facilitate module imports from the project. Specifically, it makes the modules in the `src` directory available.

The main function, `run_evaluations`, is defined to execute evaluation scripts and generate coverage reports. It accepts an optional list of directories containing evaluation files.

The `PromptCoverageTracker` and `TestLogger` classes from `src.test_utils` are initialized. Test results are saved in a timestamped results directory.

The `run_evaluations` function executes specified evaluation scripts using `pytest`, logging successes and failures, and generates coverage reports saved in JSON format. 

The script can accept additional directories through command-line arguments. It defaults to running `run_evaluations` with no additional directories if none are provided.

Generated by Salon from Braid Technologies, 06/03/2025