**Directory evals**

**classify_eval.py**

This module tests a text classification API for sports-related texts at the `/classify` endpoint. It contains three main tests to ensure accurate classification of basketball, similar basketball texts, and tennis.

Important functions: `test_basic_basketball_classification()`, `test_similar_basketball_classification()`, `test_different_sport_classification()`. Important components: `simple_test`, `mutation_test`, `variant_test`, `request_timeout`, `article_classifier_prompt_id`, `BASE_URL`, `SESSION_KEY`, `API_ENDPOINT`.

**enriched_query_api_eval_test.py**

This module validates the enriched query API endpoint `/queryModelWithEnrichment`. It examines valid and invalid requests and response accuracy.

Key functions include: `valid_request_payload()`, `invalid_request_payload()`, `test_enriched_query_invalid_payload`, `test_enriched_query_success`, and several specific scenario tests. Important components: `EnrichedQueryApi`, `valid_request_payload`, `invalid_request_payload`.

**find_theme_eval.py**

This module tests the `/findtheme` API endpoint to identify main themes in sports-related text.

Important functions: `test_basic_basketball_theme`, `test_alternative_basketball_description`, `test_football_different_theme`. Important components: `simple_test`, `mutation_test`, `variant_test`.

**generate_follow_up_question_api_eval_test.py**

Tests the `generate_follow_up_question` API. Uses fixtures and decorators to validate that the API generates relevant follow-up questions based on different summaries.

Key functions: `suggest_content`, `suggest_content_fixture`, `test_generate_follow_up_suggestions_simple_summary`, `test_generate_follow_up_suggestions_slightly_varied_summary`, `test_generate_follow_up_suggestions_significantly_varied_summary`. Important components: `EnrichedQueryApi`, `IGenerateQuestionRequest`, `IQuestionGenerationResponse`, `simple_test`, `mutation_test`, `variant_test`.

**summarise_eval.py**

Tests the `/summarize` API endpoint for sports event summarization, evaluating consistency and accuracy.

Main functions: `test_basic_sports_summary`, `test_same_game_different_wording`, `test_different_game_different_summary`. Important components: `simple_test`, `mutation_test`, `variant_test`, `BASE_URL`, `API_ENDPOINT`.

**summarise_filter_eval.py**

Tests the summarization API, including handling of reworded texts and nonsensical inputs.

Main functions: `test_basic_summary`, `test_different_summary`, `test_different_game_different_summary`. Important components: `simple_test`, `mutation_test`, `variant_test`, `requests.post`.

**Directory run_evals**

The code imports several modules, including `os`, `sys`, `pytest`, `json`, `datetime`, and `pathlib.Path`.

It adds the project root directory to the Python path for module imports from the `src` directory.

The main function, `run_evaluations`, executes evaluation scripts and generates coverage reports. It can accept an optional list of directories containing evaluation files.

The `PromptCoverageTracker` and `TestLogger` classes from `src.test_utils` are initialized. Test results are saved in a timestamped directory.

Using `pytest`, the `run_evaluations` function logs test successes and failures, generating coverage reports saved as JSON files. Command-line arguments can specify additional directories. If none, it runs `run_evaluations` with no additional directories.

**Directory src**

The code in **test_utils.py** defines a framework for testing with specialized decorators and logging capabilities.

**Key Classes:**

1. **TestType (Enum)**: Enumerates three test types: SIMPLE, MUTATION, VARIANT.
2. **TestLogger**: Manages logging for tests, including creating directories, recording test starts/ends, and saving results in JSON format.
3. **PromptCoverageTracker**: Tracks prompt metadata and coverage statistics, loading prompt data from JSON and updating coverage.

**Key Functions and Decorators:**

1. **simple_test, mutation_test, variant_test (decorators)**: These decorators log the start and end of tests, handle exceptions, and wrap functions for specific test types.

**Usage:**

- Apply decorators to test functions for automatic logging of metadata and results.
- `PromptCoverageTracker` records test runs and generates coverage reports.

**Important Methods:**

- `TestLogger.start_test` & `end_test`: Manage test start/end, recording data.
- `PromptCoverageTracker.record_test` & `get_coverage_report`: Update test records and generate reports.

Generated by Salon from Braid Technologies, 06/03/2025