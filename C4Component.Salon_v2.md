```mermaid
C4Component
title Braid Technologies Studio Component Diagram

System_Boundary(c1, "Client Applications") {
  Component(cascade, "Cascade", "Edge Plugin", "Scrapes, summarizes, and classifies web page text (Typescript)")
  Component(boxer, "Boxer", "Web App", "AI-powered learning assistant (Typescript/React)")
  Component(teams_plugin, "Teams Plugin", "Teams Integration", "Integrates Boxer and Waterfall into Teams (Typescript)")
}

System_Boundary(c2, "Server-Side Components") {
  Component(waterfall, "Waterfall", "Data Pipeline", "Downloads, summarizes, and analyzes web pages (Python)")
  Component(api, "Braid APIs", "Azure Functions", "Provides summarization, classification, and embedding services (Python)")
  Component(salon, "Salon", "Automation Scripts", "Generates test code and documentation (Python)")
}

System_Boundary(c3, "Shared Components") {
  Component(common_ts, "CommonTs", "Typescript Library", "Utility classes and API definitions (Typescript)")
  Component(common_py, "CommonPy", "Python Library", "Utility classes for API access (Python)")
}

System_Boundary(c4, "Testing & Evaluation") {
  Component(api_test, "ApiTest", "Test Suite", "Tests for Braid APIs (Python)")
  Component(boxer_eval, "BoxerEval", "Evaluation Suite", "Evaluates Boxer performance")
}

System_Ext(openai, "OpenAI API", "External Service", "Provides AI models for summarization and classification")
System_Ext(azure, "Azure", "External Service", "Cloud platform for hosting and processing")


Rel(cascade, api, "Uses", "JSON/HTTP")
Rel(waterfall, api, "Uses", "JSON/HTTP")
Rel(boxer, api, "Uses", "JSON/HTTP")
Rel(teams_plugin, boxer, "Uses")
Rel(teams_plugin, waterfall, "Uses")
Rel(salon, openai, "Uses", "API")
Rel(api, openai, "Uses", "API")
Rel(cascade, common_ts, "Uses")
Rel(waterfall, common_py, "Uses")
Rel(api, common_py, "Uses")
Rel(salon, common_py, "Uses")
Rel(api_test, api, "Tests")
Rel(boxer_eval, boxer, "Evaluates")
Rel(salon, api_test, "Generates")

Rel(api, azure, "Runs on")
Rel(waterfall, azure, "Runs on")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
