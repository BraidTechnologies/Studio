```mermaid
C4Container
title Braid Technologies Studio

Person(Developer, "Developer")

System_Boundary(BraidTechnologies, "Braid Technologies Studio") {
    Container(Cascade, "Cascade", "Edge plugin", "Scrapes web page text, summarizes it, and classifies the text into categories.")
    Container(Waterfall, "Waterfall", "Python application", "Data pipeline for downloading, summarizing, and analyzing web pages, finding common topics.")
    Container(Boxer, "Boxer", "Web application", "AI-enabled learning assistant for developers to build generative AI applications quickly, with LLM integration and enriched document links.")
    Container(Teams, "Teams", "Plugin", "Integrates Boxer & Waterfall into the Teams environment.")
    Container(BoxerEval, "BoxerEval", "Evaluation System", "Evaluations for Boxer.")

    Container_Boundary(API_Boundary, "API Layer") {
        Container(Api, "Api", "Azure Functions", "Azure hosted functions for summarizing and classifying text, as well as database and AI model access.")
        Container(ApiTest, "ApiTest", "Test Suite", "Tests the APIs from Python classes.")
    }

    Container_Boundary(Common_Boundary, "Common Libraries") {
        Container(CommonTs, "CommonTs", "TypeScript Library", "TypeScript utility classes, API definitions, and types used across clients and server.")
        Container(CommonPy, "CommonPy", "Python Library", "Python utility classes for accessing TypeScript server APIs and holding common code.")
    }

    Container(Salon, "Salon", "Script Set", "Scripts to support the engineering process, generate Python test code, and scan directories to produce text files for LLMs.")
}

System_Boundary(ExternalSystems, "External Systems") {
    System_Ext(OpenAI, "Azure OpenAI", "AI Platform", "Hosted AI models for text summarization and classification.")
}

Rel(Developer, Cascade, "Uses")
Rel(Developer, Waterfall, "Uses")
Rel(Developer, Boxer, "Uses")
Rel(Developer, Teams, "Uses")
Rel(Developer, BoxerEval, "Uses")

Rel(Cascade, Api, "Calls API for server-side keys")
Rel(Waterfall, Api, "Calls API for server-side keys")
Rel(Boxer, Api, "Calls API for server-side keys")
Rel(Waterfall, CommonPy, "Uses utility classes")
Rel(Cascade, CommonTs, "Uses utility classes")
Rel(ApiTest, Api, "Tests")

Rel(Api, OpenAI, "Calls")
Rel(Waterfall, OpenAI, "Calls")
Rel(Cascade, OpenAI, "Calls")
Rel(Boxer, OpenAI, "Calls")

Rel(Salon, ApiTest, "Generates test code")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
