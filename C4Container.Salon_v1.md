C4Context
  System_Boundary(c1, "Braid Technologies Studio") {
    Container(waterfall, "Waterfall", "Python", "Data analysis backend pipeline")
    Container(cascade, "Cascade", "Typescript/Javascript", "Edge plugin for text analysis and classification")
    Container(api, "API", "Azure Functions", "Provides summarization, classification, and embedding generation endpoints")
    Container(apitest, "ApiTest", "Python", "Test code for APIs")
    Container(commonts, "CommonTs", "Typescript", "Utility classes for API definitions and types")
    Container(commonpy, "CommonPy", "Python", "Utility classes for accessing APIs")
    Container(salon, "Salon", "Python", "Scripts for test code generation and code analysis")
    Container(boxer, "Boxer", "Typescript/Javascript", "AI-enabled learning assistant web application")
    Container(teams, "Teams", "Plugin", "Integrates Boxer and Waterfall into Teams")
    ContainerDb(a16z_cannon, "A16Z AI Cannon", "Document Database", "Stores useful material for Boxer")

    Rel(cascade, api, "Uses", "REST")
    Rel(waterfall, api, "Uses", "REST")
    Rel(boxer, api, "Uses", "REST")
    Rel(apitest, api, "Tests", "")
    Rel(salon, api, "Generates tests for", "")
    Rel(cascade, commonts, "Uses", "")
    Rel(waterfall, commonpy, "Uses", "")
    Rel(boxer, a16z_cannon, "Queries", "")
    Rel(teams, boxer, "Integrates", "")
    Rel(teams, waterfall, "Integrates", "")

  }

  Person_Ext(user, "User", "Interacts with the system")

  Rel(user, cascade, "Uses", "")
  Rel(user, boxer, "Uses", "")
  Rel(user, teams, "Uses", "")

  System_Ext(azure, "Azure", "Cloud platform")

  Rel(api, azure, "Runs on", "")
  Rel(waterfall, azure, "Runs on", "")

%% Generated by Salon from Braid Technologies, 28/02/2025
