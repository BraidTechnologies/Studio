```mermaid
C4Context
title Braid Technologies Studio Context Diagram

Person(dev, "Developer")
System_Boundary(braid, "Braid Technologies") {
    System(cascade, "Cascade", "Edge plug-in for text scraping and classification (TypeScript/JavaScript)")
    System(waterfall, "Waterfall", "Data analysis pipeline for web pages (Python)")
    System(api, "API", "Azure functions to summarize and classify text using OpenAI (TypeScript/JavaScript)")
    System(apiTest, "ApiTest", "Test code for the APIs generated by Salon (Python)")
    System(commonTs, "CommonTs", "TypeScript utility classes and API definitions")
    System(commonPy, "CommonPy", "Python utility classes for accessing server APIs")
    System(salon, "Salon", "Scripts to generate test code and text files for LLMs")
    System(boxer, "Boxer", "AI-enabled learning assistant for generative AI applications")
    System(teams, "Teams", "Teams plug-in bringing Boxer and Waterfall into Teams")
    System(boxerEval, "BoxerEval", "Evaluation scripts for Boxer")
}

System_Ext(internet, "Internet")
System_Ext(azure, "Azure", "Azure platform for hosting and processing")

Rel(dev, cascade, "Uses to scrape and classify text")
Rel(dev, waterfall, "Uses for data analysis")
Rel(dev, api, "Calls for summarization and classification")
Rel(cascade, api, "Calls for server-side keys or AI model access")
Rel(waterfall, api, "Calls for server-side keys or AI model access")
Rel(boxer, api, "Calls for server-side keys or AI model access")
Rel(dev, apiTest, "Runs tests on APIs")
Rel(dev, commonTs, "Uses utility classes")
Rel(dev, commonPy, "Uses utility classes")

Rel(api, azure, "Hosted and processed in")
Rel(apiTest, azure, "Accesses APIs hosted on")
Rel(boxer, internet, "Fetches content from")
Rel(teams, internet, "Fetches content from")
Rel(boxerEval, internet, "Fetches content from")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
