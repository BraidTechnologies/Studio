C4Context
  Person(user, "User", "Interacts with Boxer Search Interface")

  System_Boundary(c1, "Waterfall System") {
    System(waterfall, "Waterfall", "Document processing and enrichment framework")

    System_Boundary(waterfall_pipeline, "Waterfall Pipeline") {
        Component(web_searcher, "Web Searcher", "Retrieves documents using Google Search API")
        Component(summariser, "Summariser", "Summarises text using AI models")
        Component(embedder, "Embedder", "Generates text embeddings")
        Component(cluster_analyser, "Cluster Analyser", "Performs clustering on embeddings")
        Component(waterfall_pipeline_report, "Report Generator", "Generates analysis reports (HTML, JSON)")
        Component(google_office_mailer, "Emailer", "Sends email summaries")
        Component(waterfall_pipeline_save_chunks, "Chunk Saver", "Saves chunks to database")

    }

    System_Boundary(boxer_pipeline, "Boxer Pipeline") {
        Component(youtube_playlist_searcher, "Youtube Playlist Searcher", "Searches for YouTube playlists")
        Component(youtube_transcript_downloader, "YouTube Transcript Downloader", "Downloads YouTube transcripts")
        Component(youtube_transcript_chunker, "YouTube Transcript Chunker", "Chunks YouTube transcripts")
        Component(html_link_crawler, "HTML Link Crawler", "Crawls web pages for links")
        Component(html_file_downloader, "HTML File Downloader", "Downloads HTML content")
        Component(boxer_sources, "Boxer Sources", "Provides curated list of educational resources")
        Component(chunker, "Chunker", "Segments text into smaller parts")
        Component(embedder_b, "Embedder", "Generates text embeddings", "Same as embedder in Waterfall Pipeline. Duplicated for clarity within Boxer Pipeline.")
        Component(boxer_save_chunks, "Chunk Saver", "Saves chunks to database. Same functionality as in Waterfall Pipeline, but applied within the Boxer context.", "Same as Waterfall Chunk Saver. Duplicated for clarity within Boxer Pipeline.")
      
    }
    Rel(waterfall_pipeline, db_repository, "Saves chunks to", "BraidApis Database")
    Rel(boxer_pipeline, db_repository, "Saves chunks to", "BraidApis Database")

    Component(db_repository, "DbRepository", "Stores data in BraidApis database")
    Component(file_repository, "FileRepository", "Stores data in local file system")
    Component(embedding_finder, "EmbeddingFinder", "Finds nearest embeddings")
    Component(theme_finder, "ThemeFinder", "Identifies themes using external API")


  }

  System_Ext(google_search_api, "Google Search API", "Provides search results")
  System_Ext(youtube_api, "YouTube API", "Provides video and playlist data")
  System_Ext(external_api, "External API", "Provides chunking and theme identification services")
  System_Ext(braidapis_db, "BraidApis Database (Chunk table)", "Stores chunk data")


  Rel(user, boxer_pipeline, "Searches content via", "Boxer Search Interface")
  Rel(waterfall_pipeline, google_search_api, "Uses", "")
  Rel(boxer_pipeline, youtube_api, "Uses", "")
  Rel(chunker, external_api, "Uses", "")
  Rel(theme_finder, external_api, "Uses", "")
  Rel(waterfall, file_repository, "Uses", "")

%% Generated by Salon from Braid Technologies, 28/02/2025
