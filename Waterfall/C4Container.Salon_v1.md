```mermaid
C4Container
title Waterfall System Container Diagram

Person(user, "User", "Interacts with the Waterfall system to retrieve and analyze AI/ML educational content")

System_Boundary(Waterfall_Boundary, "Waterfall System") {
    
    Container(WaterfallPipeline, "Waterfall Pipeline", "Python", "Retrieves documents using the Google Search API, generates AI-powered summaries, calculates embeddings, clusters content, and generates reports")
    Container(BoxerPipeline, "Boxer Pipeline", "Python", "Builds a knowledge base by processing YouTube and web content, generating summaries, embeddings, and powering the semantic search interface")
    Container(DbRepository, "Database Repository", "Database", "Stores data in the BraidApis database")
    Container(FileRepository, "File Repository", "Local File System", "Stores data in the local file system")
    Container(HtmlDownloader, "HTML File Downloader", "Python", "Downloads and processes HTML content from web pages")
    Container(YoutubeDownloader, "YouTube Transcript Downloader", "Python", "Downloads transcripts of YouTube videos")
    Container(EmailService, "Email Service", "Python", "Sends automated reports via email")
    Container(ClusterAnalyser, "Cluster Analyser", "Python", "Performs clustering of document embeddings")
    Container(Summariser, "Summariser", "Python", "Generates text summaries using AI models")
    Container(Embedder, "Embedder", "Python", "Generates embeddings for semantic search and clustering")
    Container(ThemeFinder, "Theme Finder", "Python", "Identifies themes in text content using an external API")
    Container(WebSearcher, "Web Searcher", "Python", "Uses the Google Custom Search Engine API to find relevant URLs")
}

System_Ext(GoogleSearchAPI, "Google Search API", "External System", "Provides search results for given queries")
System_Ext(YoutubeAPI, "YouTube API", "External System", "Provides access to video transcripts and playlists")
System_Ext(ExternalEmailAPI, "Email API", "External System", "Handles email sending functionality")
System_Ext(ExternalAPI, "External API", "External System", "Handles external API calls for theme identification")

Rel(user, WaterfallPipeline, "Interacts with")
Rel(user, BoxerPipeline, "Interacts with")

Rel(WaterfallPipeline, GoogleSearchAPI, "Retrieves documents from")
Rel(WaterfallPipeline, EmailService, "Sends reports via")
Rel(WaterfallPipeline, Summariser, "Generates summaries")
Rel(WaterfallPipeline, Embedder, "Generates embeddings")
Rel(WaterfallPipeline, WebSearcher, "Finds relevant URLs using")
Rel(WaterfallPipeline, ClusterAnalyser, "Performs clustering with")
Rel(WaterfallPipeline, ThemeFinder, "Identifies themes with")

Rel(BoxerPipeline, YoutubeAPI, "Downloads transcripts from", "Reads playlists from")
Rel(BoxerPipeline, HtmlDownloader, "Downloads web content using")
Rel(BoxerPipeline, Summariser, "Generates summaries using")
Rel(BoxerPipeline, Embedder, "Generates embeddings using")

Rel(DbRepository, WaterfallPipeline, "Stores processed data into")
Rel(DbRepository, BoxerPipeline, "Stores processed data into")
Rel(FileRepository, WaterfallPipeline, "Stores processed data into")
Rel(FileRepository, BoxerPipeline, "Stores processed data into")

Rel(ClusterAnalyser, WaterfallPipeline, "Clusters content for")
Rel(ClusterAnalyser, BoxerPipeline, "Clusters content for")
Rel(Summariser, WaterfallPipeline, "Generates summaries for")
Rel(Summariser, BoxerPipeline, "Generates summaries for")
Rel(Embedder, WaterfallPipeline, "Generates embeddings for")
Rel(Embedder, BoxerPipeline, "Generates embeddings for")
Rel(ThemeFinder, WaterfallPipeline, "Identifies themes for")
Rel(WebSearcher, WaterfallPipeline, "Searches web content for")

Rel(EmailService, ExternalEmailAPI, "Sends emails via")
Rel(ThemeFinder, ExternalAPI, "Calls external API for theme identification")

Rel(HtmlDownloader, ExternalAPI, "Downloads content from")
Rel(YoutubeDownloader, ExternalAPI, "Downloads transcripts from")

Rel(HardwareResource, SoftwareResource, IssueIn, "Connectivity Issue Occurred while connecting dashboard API endpoint")

Rel(webapp (HTML File Downloader), request/response, Browser/Server Structure)
Rel(youtube_searcher (HTML File Downloader), request/response, Browser/Server Structure)
Rel(youtube_transcript_chunker (HTML File Downloader), request/response, Browser/Server Structure)

Rel(webapp (HTML File Downloader), request/response, Browser/Server Structure)
Rel(youtube_searcher (HTML File Downloader), request/response, Browser/Server Structure)
Rel(youtube_transcript_chunker (HTML File Downloader), request/response, Browser/Server Structure)

Rel(FacebookAPI, Python Web App, API Call, "REST API", API Call)
Rel(TwitterAPI, Web App, "Open API", "REST API", API)
Rel(YoutubeAPI, YoutubePlayer, "Video Streaming", "Player Embed")
Rel(Blogger API, "API", User Experience, API Encoder)
```

%% Generated by Salon from Braid Technologies, 09/03/2025
