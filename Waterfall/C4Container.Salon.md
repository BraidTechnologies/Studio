```mermaid
C4Container
System_Boundary(c1, "Waterfall System") {
  Container(waterfall_pipeline, "Waterfall Pipeline", "Python", "Retrieves documents, generates summaries, performs clustering, and sends reports.")
  Container(boxer_pipeline, "Boxer Pipeline", "Python", "Builds knowledge base from web and YouTube content, powers semantic search.")
  ContainerDb(braid_apis_db, "BraidApis Database", "Cosmos DB", "Stores document chunks and metadata.")
  Container(reporting, "Reporting", "Python", "Generates HTML/JSON reports and sends email notifications.")
  Container(boxer_search_interface, "Boxer Search Interface", "Python/Web", "Provides semantic search functionality using Boxer pipeline data.")
}

System_Ext(google_search_api, "Google Search API", "Provides search results for document retrieval.")
System_Ext(web_urls, "Web URLs", "Source of HTML documents and content.")
System_Ext(youtube, "YouTube", "Source of video playlists and transcripts.")
System_Ext(external_api, "External API", "Provides chunking, summarization, and embedding services.")
System_Ext(email_server, "Email Server", "Sends email notifications.")
System_Ext(local_file_system, "Local File System", "Temporary storage for downloaded files.")

Rel(waterfall_pipeline, google_search_api, "Retrieves documents", "HTTPS")
Rel(waterfall_pipeline, braid_apis_db, "Stores and retrieves chunks", "Database connection")
Rel(waterfall_pipeline, reporting, "Sends analysis reports", "Internal API")
Rel(boxer_pipeline, web_urls, "Downloads HTML content", "HTTPS")
Rel(boxer_pipeline, youtube, "Downloads playlists and transcripts", "HTTPS")
Rel(boxer_pipeline, braid_apis_db, "Stores and retrieves chunks", "Database connection")
Rel(boxer_pipeline, boxer_search_interface, "Provides data for semantic search", "Internal API")
Rel(boxer_pipeline, external_api, "Summarization and Embedding", "HTTPS")
Rel(reporting, email_server, "Sends email notifications", "SMTP")
Rel(reporting, local_file_system, "Stores temporary report files", "File I/O")
Rel(waterfall_pipeline, external_api, "Chunking, Summarization, Embedding", "HTTPS")
Rel(waterfall_pipeline, local_file_system, "Temporary file storage", "File I/O")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
