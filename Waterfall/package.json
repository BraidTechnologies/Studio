{
   "name": "waterfall",
   "version": "1.0.0",
   "description": "",
   "main": "public/payload.js",
   "scripts": {
      "package": "copy dist\\src\\payload.js public",   
      "build": "npm run compile && npm run package",      
      "compile": "tsc -b",
      "clean": "rimraf dist",
      "prestart": "npm run clean && npm run build",
      "test-mini": "mocha --debug-brk --require ts-node/register -g Caucus*"
   },
   "dependencies": {
      "axios": "^1.7.2"
   },
   "devDependencies": {
      "tsc": "^2.0.4",
      "typescript": "^5.5.3"
   }
}
