C4Context
  Person(user, "User", "Interacts with the Boxer search interface")

  System_Boundary(c1, "Waterfall System") {
    Container(waterfall_pipeline, "Waterfall Pipeline", "Python", "Retrieves documents, generates summaries and embeddings, performs clustering, and generates reports. Sends email summaries.")
    Container(boxer_pipeline, "Boxer Pipeline", "Python", "Builds knowledge base from web URLs and YouTube playlists. Powers semantic search in Boxer interface.")
    Container(chunker, "Chunker", "Python", "Segments text into smaller parts.")
    Container(cluster_analyser, "ClusterAnalyser", "Python", "Performs KMeans clustering on embeddings.")
    Container(db_repository, "DbRepository", "Python", "Interacts with the BraidApis database (Chunk table).")
    Container(embedder, "Embedder", "Python", "Creates text embeddings.")
    Container(embedding_finder, "EmbeddingFinder", "Python", "Finds nearest embeddings using cosine similarity.")
    Container(embedding_repository, "Embedding Repository", "Local File System", "Stores embeddings.")
    Container(file_repository, "File Repository", "Local File System", "Generic file operations.")
    Container(html_file_downloader, "HtmlFileDownloader", "Python", "Downloads HTML content from web pages.")
    Container(html_link_crawler, "HtmlLinkCrawler", "Python", "Crawls web pages for links.")
    Container(summariser, "Summariser", "Python", "Creates text summaries.")
    Container(summarise_fail_suppressor, "SummariseFailSuppressor", "Python", "Suppresses invalid summaries.")
    Container(summary_repository, "Summary Repository", "Local File System", "Stores text summaries.")
    Container(text_repository, "Text Repository", "Local File System", "Stores text content.")
    Container(theme_finder, "ThemeFinder", "Python", "Identifies themes using an external API.")
    Container(web_searcher, "WebSearcher", "Python", "Uses Google Custom Search Engine API.")
    Container(youtube_playlist_searcher, "YoutubePlaylistSearcher", "Python", "Retrieves YouTube playlist information.")
    Container(youtube_transcript_chunker, "YouTubeTranscriptChunker", "Python", "Chunks YouTube transcripts.")
    Container(youtube_transcript_downloader, "YouTubeTranscriptDownloader", "Python", "Downloads YouTube transcripts.")
    Container(boxer_interface, "Boxer Interface", "User Interface", "Provides semantic search functionality.")
    Container(reporting, "Reporting", "Python", "Generates and sends reports (HTML, JSON, Email).")

  }

  System_Ext(google_search_api, "Google Search API", "Provides search results.")
  System_Ext(external_api_chunker, "External Chunking API", "Provides text chunking functionality.")
  System_Ext(external_api_summariser, "External Summarisation API", "Provides text summarisation functionality.")
  System_Ext(external_api_embedder, "External Embedding API", "Provides text embedding functionality.")
  System_Ext(external_api_theme, "External Theme API", "Provides theme identification functionality.")
  System_Ext(youtube_api, "YouTube Data API", "Provides access to YouTube data.")
  System_Ext(braid_apis_db, "BraidApis Database", "Stores chunk data.")
  System_Ext(email_server, "Email Server", "Sends email notifications.")


  Rel(user, boxer_interface, "Searches", "UI")
  Rel(boxer_pipeline, boxer_interface, "Provides data", "")
  Rel(waterfall_pipeline, google_search_api, "Uses", "")
  Rel(waterfall_pipeline, reporting, "Generates and sends", "")
  Rel(boxer_pipeline, youtube_api, "Uses", "")
  Rel(chunker, external_api_chunker, "Uses", "")
  Rel(summariser, external_api_summariser, "Uses", "")
  Rel(embedder, external_api_embedder, "Uses", "")
  Rel(theme_finder, external_api_theme, "Uses", "")
  Rel(db_repository, braid_apis_db, "Reads/Writes", "")
  Rel(reporting, email_server, "Sends emails", "SMTP")
  Rel(waterfall_pipeline, db_repository, "Uses", "")
  Rel(boxer_pipeline, db_repository, "Uses", "")

%% Generated by Salon from Braid Technologies, 28/02/2025
