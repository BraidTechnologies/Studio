```mermaid
C4Context
title Waterfall: AI-Powered Document Processing and Enrichment Framework

Person(user, "User", "Researcher seeking AI/ML educational content")
System(waterfall, "Waterfall", "Python framework for processing and enriching AI/ML educational documents")

System_Ext(google_search, "Google Search API", "Provides URLs for relevant documents")
System_Ext(youtube, "YouTube", "Source of video playlists and transcripts")
System_Ext(web, "Web", "Source of articles, tutorials, and documentation")
System_Ext(email, "Email Server", "Sends summary reports to leadership")
System_Ext(braid_apis, "BraidApis Database", "Stores document chunks and metadata")
System_Ext(external_api_chunker, "External Chunking API", "Divides text into smaller segments")
System_Ext(external_api_summariser, "External Summarization API", "Generates text summaries")
System_Ext(external_api_embedder, "External Embedding API", "Creates text embeddings")
System_Ext(external_api_theme, "External Theme API", "Identifies themes from text")
System_Ext(external_api_suppress, "External Suppression API", "Validates text summaries")


Rel(user, waterfall, "Searches for and analyzes AI/ML content", "Boxer Interface")
Rel(waterfall, google_search, "Retrieves documents", "API")
Rel(waterfall, youtube, "Downloads playlists and transcripts", "API")
Rel(waterfall, web, "Crawls and downloads content", "HTTPS")
Rel(waterfall, email, "Sends summary reports", "SMTP")
Rel(waterfall, braid_apis, "Stores and retrieves document chunks", "API")
Rel(waterfall, external_api_chunker, "Sends text for chunking", "API")
Rel(waterfall, external_api_summariser, "Sends text for summarization", "API")
Rel(waterfall, external_api_embedder, "Generates text embeddings", "API")
Rel(waterfall, external_api_theme, "Identifies themes", "API")
Rel(waterfall, external_api_suppress, "Validates summaries", "API")

System_Boundary(c1, "Waterfall Framework") {
  Container(waterfall_pipeline, "Waterfall Pipeline", "Python", "Retrieves, summarizes, and clusters documents")
  Container(boxer_pipeline, "Boxer Pipeline", "Python", "Builds and powers the Boxer knowledge base")

  ContainerDb(braid_db, "Chunk Table", "BraidApis Database", "Stores document chunks")

  Rel(waterfall_pipeline, google_search, "Retrieves documents", "API")
  Rel(waterfall_pipeline, external_api_summariser, "Generates summaries", "API")
  Rel(waterfall_pipeline, external_api_embedder, "Generates embeddings", "API")
  Rel(waterfall_pipeline, braid_db, "Stores and retrieves chunks", "API")
  Rel(waterfall_pipeline, email, "Sends summary reports", "SMTP")

  Rel(boxer_pipeline, youtube, "Downloads playlists and transcripts", "API")
  Rel(boxer_pipeline, web, "Crawls and downloads content", "HTTPS")
  Rel(boxer_pipeline, external_api_chunker, "Chunks text", "API")
  Rel(boxer_pipeline, external_api_summariser, "Generates summaries", "API")
  Rel(boxer_pipeline, external_api_embedder, "Generates embeddings", "API")
  Rel(boxer_pipeline, braid_db, "Stores and retrieves chunks", "API")
}

```

%% Generated by Salon from Braid Technologies, 28/02/2025
