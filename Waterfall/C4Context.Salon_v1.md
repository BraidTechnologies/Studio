```mermaid
C4Context
    title System Context diagram for Waterfall

    Person(User, "End User")
    System_Boundary(WaterfallBoundary, "Waterfall System") {
        System(WaterfallSystem, "Waterfall", "A Python framework designed for processing documents using an AI enrichment pipeline.")
        
        System_Boundary(WaterfallPipeline, "Waterfall Pipeline") {
            System(WaterfallPipelineMain, "Waterfall Pipeline", "Retrieves documents using the Google Search API, generates summaries, embeddings, performs clustering, and sends email summaries.")
            System(WaterfallPipelineSearchAPI, "WebSearcher", "Uses Google Custom Search API.")
            System(WaterfallPipelineSummariser, "Summariser", "Generates summaries for text.")
            System(WaterfallPipelineEmbedder, "Embedder", "Generates text embeddings.")
            System(WaterfallPipelineClusterer, "ClusterAnalyser", "Performs KMeans clustering.")
            System(WaterfallPipelineReporter, "Reporter", "Generates reports and sends emails.")
        }

        System_Boundary(BoxerPipeline, "Boxer Pipeline") {
            System(BoxerPipelineMain, "Boxer Pipeline", "Builds a knowledge base by downloading and processing content from web URLs and YouTube playlists.")
            System(BoxerPipelineSearchAPI, "YoutubePlaylistSearcher", "Searches YouTube playlists and retrieves transcripts.")
            System(BoxerPipelineHTML, "HtmlLinkCrawler", "Crawls web pages to extract sub-links.")
            System(BoxerPipelineTranscripts, "YouTubeTranscriptChunker", "Chunks YouTube transcripts.")
            System(BoxerPipelineSummariser, "Summariser", "Generates summaries for text.")
            System(BoxerPipelineEmbedder, "Embedder", "Generates text embeddings.")
        }
    }

    System_Ext(GoogleAPI, "Google API", "Uses Google Custom Search Engine API")
    System_Ext(ExternalAPI, "External AI Model API", "Provides text chunking, summarisation, embedding, and theme finding services.")
    System_Ext(EmailSystem, "Email Service", "Sends automated reports via email.")
    System_Ext(Database, "Chunk Database", "Stores chunked documents and embeddings.")

    Rel(User, WaterfallSystem, "Uses")
    Rel(WaterfallSystem, GoogleAPI, "Fetches search results")
    Rel(WaterfallSystem, ExternalAPI, "Fetches AI processing results")
    Rel(WaterfallSystem, Database, "Stores and retrieves chunks and embeddings.")
    Rel(WaterfallSystem, EmailSystem, "Sends email summaries")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
