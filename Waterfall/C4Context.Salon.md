```mermaid
C4Context
title Waterfall AI Enrichment Pipeline

Person(user, "User", "Researcher or learner using the Boxer search interface")
System(waterfall, "Waterfall", "Python framework for AI-powered document processing and enrichment")

System_Ext(google_search, "Google Search API", "Provides search results for document retrieval")
System_Ext(youtube, "YouTube", "Source of video playlists and transcripts")
System_Ext(external_api, "External APIs", "Provides services for chunking, summarization, embedding, and theme identification")
System_Ext(braid_db, "BraidApis Database", "Stores processed document chunks")
System_Ext(email, "Email Server", "Sends email summaries to leadership")
System_Ext(boxer, "Boxer", "Search interface powered by Waterfall's knowledge base")


Rel(user, boxer, "Searches for content", "HTTPS")
Rel(waterfall, google_search, "Retrieves documents", "HTTPS")
Rel(waterfall, youtube, "Downloads playlists and transcripts", "HTTPS")
Rel(waterfall, external_api, "Calls APIs for chunking, summarizing, embedding, and theme finding", "HTTPS")
Rel(waterfall, braid_db, "Stores and retrieves document chunks", "Database connection")
Rel(waterfall, email, "Sends email summaries", "SMTP")
Rel(waterfall, boxer, "Provides enriched data for semantic search", "API")

System_Boundary(c1, "Waterfall Framework") {
  Container(waterfall_pipeline, "Waterfall Pipeline", "Python", "Processes documents from Google Search")
  Container(boxer_pipeline, "Boxer Pipeline", "Python", "Processes content from URLs and YouTube")
  Container(chunker, "Chunker", "Python", "Segments text into smaller parts")
  Container(cluster_analyser, "ClusterAnalyser", "Python", "Performs KMeans clustering")
  Container(db_repository, "DbRepository", "Python", "Interacts with BraidApis database")
  Container(embedder, "Embedder", "Python", "Creates text embeddings")
  Container(embedding_finder, "EmbeddingFinder", "Python", "Finds nearest embeddings")
  Container(file_repository, "FileRepository", "Python", "Manages local file storage")
  Container(html_file_downloader, "HtmlFileDownloader", "Python", "Downloads HTML content")
  Container(html_link_crawler, "HtmlLinkCrawler", "Python", "Crawls web pages for links")
  Container(summariser, "Summariser", "Python", "Creates text summaries")
  Container(theme_finder, "ThemeFinder", "Python", "Identifies themes")
  Container(web_searcher, "WebSearcher", "Python", "Uses Google Custom Search API")
  Container(youtube_playlist_searcher, "YoutubePlaylistSearcher", "Python", "Retrieves YouTube playlist information")
  Container(youtube_transcript_chunker, "YouTubeTranscriptChunker", "Python", "Chunks YouTube transcripts")
  Container(youtube_transcript_downloader, "YouTubeTranscriptDownloader", "Python", "Downloads YouTube transcripts")
}
```

%% Generated by Salon from Braid Technologies, 28/02/2025
