```mermaid
C4Component
System_Boundary(c1, "Braid Technologies Studio") {
  Component(api, "API", "Azure Functions", "Provides summarization, classification, and embedding services; access to OpenAI models and database.")
  Component(waterfall, "Waterfall", "Python", "Data analysis backend pipeline for web page processing and cluster analysis.")
  Component(cascade, "Cascade", "Typescript/Javascript", "Edge plugin for web page scraping, summarization, and classification.")
  Component(boxer, "Boxer", "Web Frontend (Typescript)", "AI-enabled learning assistant, queries LLM and enriches with relevant document chunks.")
  Component(salon, "Salon", "Python Scripts", "Code generation and analysis tools (test code generation, repository summarization, C4 diagram generation).")
  ComponentDb(cosmosdb, "Cosmos DB", "Azure Cosmos DB", "Stores application data.")
  Component_Ext(openai, "OpenAI API", "External Service", "Provides LLM capabilities for summarization, classification, and embedding generation.")

  Rel(waterfall, api, "Uses", "REST API")
  Rel(cascade, api, "Uses", "REST API")
  Rel(boxer, api, "Uses", "REST API")
  Rel(api, cosmosdb, "Reads/Writes", "")
  Rel(api, openai, "Uses", "API")
  Rel(salon, openai, "Uses", "API for code generation")

  System_Boundary(c2, "Testing Tools") {
    Component(apitest, "ApiTest", "Python", "Generated code by Salon for testing APIs.")
    Rel(apitest, api, "Tests", "")
  }
  
  System_Boundary(c3, "Shared Components") {
    Component(commonts, "CommonTs", "Typescript", "Utility classes, API definitions, and types.")
    Component(commonpy, "CommonPy", "Python", "Utility classes for accessing Typescript APIs.")
    Rel(cascade, commonts, "Uses", "")
    Rel(waterfall, commonpy, "Uses", "")
    Rel(commonpy, api, "Uses", "REST API")
  }

  System_Boundary(c4, "Integrations") {
    Component(teams, "Teams Plugin", "Integrates Boxer & Waterfall into Teams environment.")
    Rel(teams, boxer, "Uses", "")
    Rel(teams, waterfall, "Uses", "")
  }

  System_Boundary(c5, "Evaluation Tools") {
    Component(boxereval, "BoxerEval", "Evaluation tools for Boxer.")
    Rel(boxereval, boxer, "Evaluates", "")
  }
}
```

%% Generated by Salon from Braid Technologies, 28/02/2025
