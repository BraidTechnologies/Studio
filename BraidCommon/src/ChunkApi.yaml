openapi: 3.0.0
info:
  title: Converted from ChunkApi.yaml with typeconv
  version: '1'
  x-id: ChunkApi.yaml
  x-comment: >-
    Generated from src\ChunkApi.ts by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    IChunkRequest:
      properties:
        text:
          title: IChunkRequest.text
          type: string
        chunkSize:
          title: IChunkRequest.chunkSize
          type: number
        overlapWords:
          title: IChunkRequest.overlapWords
          type: number
      required:
        - text
        - chunkSize
        - overlapWords
      additionalProperties: false
      title: IChunkRequest
      description: >-
        Interface for defining the specifications of a chunk.

        @property {string} text - The text content of the chunk.

        @property {number | undefined} chunkSize - The size of the chunk, if
        specified.
      type: object
