```mermaid
  C4Container
  title Salon System Architecture

  Person(admin, "Administrator", "Administers and monitors the automated tools.")
  Person(dev, "Developer", "Uses the automated tools to generate tests and analyze code.")

  System_Boundary(salon, "Salon System") {
    Container(api_to_test_code, "api_to_test_code", "Python Script", "Generates Python test code from API specifications.")
    Container(repo_to_text, "repo_to_text", "Python Script", "Processes and analyzes codebases, concatenates source files into text files.")
    Container(repo_to_c4, "repo_to_c4", "Python Script", "Generates C4 architecture diagrams from GitHub repositories.")
    ContainerDb(logging, "Logging System", "Database", "Stores logs and error reports for analysis.")
    Container(openai_api, "OpenAI API", "API", "Provides AI-driven responses for test and code generation.")
  }

  System_Ext(github, "GitHub", "Source code hosting service")

  Rel(admin, api_to_test_code, "Manages and monitors")
  Rel(admin, repo_to_text, "Manages and monitors")
  Rel(admin, repo_to_c4, "Manages and monitors")
  Rel(dev, api_to_test_code, "Uses for generating tests")
  Rel(dev, repo_to_text, "Uses for analyzing code")
  Rel(dev, repo_to_c4, "Uses for generating diagrams")
  
  Rel(api_to_test_code, openai_api, "Sends prompts for test generation")
  Rel(api_to_test_code, logging, "Logs generation process")
  
  Rel(repo_to_text, openai_api, "Requests code summaries for analysis")
  Rel(repo_to_text, github, "Processes repository content")
  Rel(repo_to_text, logging, "Logs transformation process")
  
  Rel(repo_to_c4, openai_api, "Requests code summaries and diagram information")
  Rel(repo_to_c4, github, "Processes repository content")
  Rel(repo_to_c4, logging, "Logs diagram generation process")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
