**base.py**

This code defines an abstract base class `AIModel` for AI models, using Python's `abc` module.  It includes an abstract method `generate_content` which subclasses must implement. This method takes text input, a persona, optional persona introduction, desired length, and retry parameters.  The `@abstractmethod` decorator ensures that any concrete AI model inheriting from this class provides a specific implementation for generating content.  Type hints improve code readability and maintainability.


**gemini.py**

The `gemini.py` module provides a `GeminiModel` class for interacting with Google's Gemini AI model.  It abstracts content generation, handling API key setup and retries.  The `generate_content` method takes a prompt, persona information, and target length, constructs a formatted prompt, and sends it to the Gemini API.  It includes retry logic with exponential backoff to handle potential transient errors.  A developer API key is required and should be stored in the `GOOGLE_DEVELOPER_API_KEY` environment variable.  The module uses the `google-genai` library for Gemini interaction.


**model_factory.py**

This code defines a factory function `create_model` that instantiates different language models based on a string input `model_type`. It uses an enum `ModelType` to represent the available model types (BRAID_API and LOCAL_GEMINI).  The function converts the input string to the corresponding enum value.  If the type is BRAID_API, it returns an `OpenAiModel`; if it's LOCAL_GEMINI, it returns a `GeminiModel`. If the `model_type` is invalid, it raises a ValueError. This factory pattern simplifies model creation by abstracting the specific model instantiation logic.


**open_ai.py**

This code defines an `OpenAiModel` class to interact with the OpenAI API and a supplementary summarization endpoint.  It retrieves the OpenAI API key and other settings from environment variables.  The `generate_content` method sends a summarization request to a specified endpoint, handling retries and errors. It constructs a prompt from provided text, persona, and persona introduction. The  `generate_code` method uses the OpenAI Assistants API with a code interpreter tool to generate code from a given prompt and instructions, returning the generated Python code.  Both methods include error handling and logging.


Generated by Salon from Braid Technologies, 27/02/2025