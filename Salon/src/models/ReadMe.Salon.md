**base.py**

This code defines an abstract base class `AIModel` for AI models, using Python's `abc` module.  It includes an abstract method `generate_content` which subclasses must implement. This method takes text input, a persona, optional persona introduction, desired length, retry attempts, and delay between retries. It's type-hinted to return an optional string, allowing for cases where content generation might fail.  The `NotImplementedError` ensures that any subclass must provide a concrete implementation for  `generate_content`.


**gemini.py**

The `gemini.py` module provides a `GeminiModel` class for interacting with Google's Gemini API.  It initializes with a developer key, optionally configuring a proxy for requests. The core function, `generate_content`, constructs a prompt incorporating persona information and text, then sends it to the Gemini API.  It implements retry logic for handling network issues and API errors, returning the generated text or `None` if errors occur.  The module requires the `GOOGLE_DEVELOPER_API_KEY` environment variable to function correctly.


**model_factory.py**

This code defines a factory function `create_model` that instantiates different language models based on the input `model_type` string.  It uses an enumeration `ModelType` to represent the available model types (BRAID_API and LOCAL_GEMINI).  The function converts the input string to the corresponding enum value. Based on this value, it returns an instance of either `OpenAiModel` for BRAID_API or `GeminiModel` for LOCAL_GEMINI.  If the provided `model_type` is invalid, the function raises a ValueError.


**open_ai.py**

This code defines an `OpenAiModel` class to interact with OpenAI and Braid APIs. It retrieves the OpenAI API key and Braid URL and session key from environment variables.  The `generate_content` method sends a summarization request to the Braid API with specified persona, text, and length. It includes retry logic for handling API request failures. The `generate_code` method uses the OpenAI Assistants API with a code interpreter tool to generate Python code based on a given prompt and instructions.  It retrieves and returns the generated code from the API response.  Both methods include error handling and logging.


Generated by Salon from Braid Technologies, 28/02/2025