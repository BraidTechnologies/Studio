**base.py**

This code defines a base class `DirectoryProcessor` for processing directory data.  Subclasses must implement the `visit` method to perform specific actions on a `DirectoryData` object.  The `process_directory` function recursively traverses a directory structure. For each directory, it first processes its subdirectories and then applies a list of `DirectoryProcessor` instances to the current directory. If a processor's `visit` method returns a Path, processing stops for that directory and the Path is added to the current directory's source files. This allows processors to act as filters, generators, or transformers of directory content.


**c4_generator.py**

The `C4Generator` class processes directories to create C4 architecture diagrams.  It searches for `readme.md` in the root and `readme.salon.md` within subdirectories (excluding "test"). If found, it combines their content.  Using an AI model, it generates Mermaid-format C4 Context, Container, and Component diagrams based on the combined readme text.  The generated diagrams are then saved as `.md` files in the root directory.  A `FileHandler` is used for file reading and writing.


**code_aggregator.py**

This code defines a `CodeAggregator` class that processes directories, reads files, and concatenates their contents into large text blocks.  It uses NLTK for word counting and splits the output into multiple files once a maximum word limit (default 200,000) is reached.  The class handles UnicodeDecodeErrors and IOErrors gracefully.  It also includes logic to avoid processing duplicate files within a specific directory named "common_dir."  Output files are named `repo_content_#.txt` and are saved in a specified or default output directory.  Each file block includes a separator and the file's relative path.


**directory_walker.py**

This code defines a function `walk_directory` that recursively explores a directory tree.  It starts at a given `root_path` and uses `Path.iterdir()` to traverse.  The function builds a `DirectoryData` object representing the directory structure.  It allows skipping specified directories and filtering files based on `skip_patterns` and `source_patterns`.  The resulting `DirectoryData` contains lists of all files, source files (matching `source_patterns`), subdirectories, and a flag indicating the presence of a "ReadMe.Salon.md" file.


**factory.py**

This Python code defines a factory, `visitors_factory.py`, which provides methods to create lists of `DirectoryProcessor` objects for different workflows.  It avoids hardcoding these lists in main scripts.  `getProcessorsRepoToText` creates processors for extracting textual information from a repository, including a `ReadmeGenerator` (and optionally a `C4Generator` and `CodeAggregator`).  `getProcessorsRepoToC4` creates processors for generating C4 model diagrams from the repository, using `ReadmeGenerator` and `C4Generator`.  The returned lists are sorted by priority, ensuring execution order.


**repo_readme_generator.py**

This code defines a `ReadmeGenerator` class that automatically creates or updates `ReadMe.Salon.md` files within directories. It checks if source files are newer than the existing readme. If so, it uses a specified AI model to summarize each file's code (if over 250 characters) and compiles these summaries into the readme.  A timestamp is used to avoid unnecessary regeneration. The `FileHandler` class handles file reading and writing.  The generated readme includes a comment indicating its automated origin and generation date.


Generated by Salon from Braid Technologies, 27/02/2025