```mermaid
C4Context
title Salon - Automated Software Development Demonstrator

Person(user, "User", "Software Developer")

System_Boundary(c1, "Salon") {
  System(apitest, "ApiTest", "API Testing Tool")
  System(codeanalysis, "CodeAnalysis", "Code Analysis Tool")
}

System_Ext(openai, "OpenAI", "Provides LLM Services")

Rel(user, apitest, "Uses", "CLI/GUI")
Rel(user, codeanalysis, "Uses", "CLI")
Rel(apitest, openai, "Uses", "API")
Rel(codeanalysis, openai, "Uses", "API")

System_Boundary(c2, "ApiTest Components") {
  Component(apitestcode, "api_to_test_code", "Generates Python test code from API specifications", "Python")
}

System_Boundary(c3, "CodeAnalysis Components") {
  Component(repototext, "repo_to_text", "Processes and analyzes codebases", "Python")
  Component(repotoc4, "repo_to_c4.py", "Generates C4 diagrams from repositories", "Python")
}

Rel(apitest, apitestcode, "Uses")
Rel(codeanalysis, repototext, "Uses")
Rel(codeanalysis, repotoc4, "Uses")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
