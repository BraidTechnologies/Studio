```mermaid
C4Context
title Salon - Automated Software Development Demonstrator

Person(user, "User", "Uses Salon tools for software development")

System_Boundary(c1, "Salon") {
  Rel(user, api_to_test_code, "Uses", "HTTP")
  Rel(user, repo_to_text, "Uses", "Command Line")
  Rel(user, repo_to_c4, "Uses", "Command Line")

  System(api_to_test_code, "api_to_test_code", "Generates Python test code from API specifications (JSON/YAML) using OpenAI")
  System(repo_to_text, "repo_to_text", "Processes codebases into text files for LLM analysis")
  System(repo_to_c4, "repo_to_c4.py", "Generates C4 diagrams from GitHub repositories")
}

System_Ext(openai, "OpenAI API", "Provides LLM capabilities for code generation and analysis")

Rel(api_to_test_code, openai, "Uses", "API")
Rel(repo_to_text, openai, "Uses", "API")
Rel(repo_to_c4, openai, "Uses", "API")

System_Ext(github, "GitHub", "Code repository")

Rel(repo_to_c4, github, "Interacts with", "API")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
