```mermaid
C4Context
    title Salon Automated Software Development Tools

    Person(user, "User", "Developer working with APIs and codebases")

    System(salon, "Salon", "System for automated software development using LLMs") {
        Container(api_to_test_code, "API to Test Code", "Python Tool", "Generates Python test code from API specifications.")
        Container(repo_to_text, "Repo to Text", "Python Tool", "Processes and analyzes codebases.")
        Container(repo_to_c4, "Repo to C4", "Python Tool", "Generates C4 architecture diagrams from GitHub repositories.")
    }
    
    System_Ext(openai, "OpenAI", "External Service", "Service for context-aware prompts and code summarization")
    System_Ext(github, "GitHub", "External Service", "Source code repository hosting service")
    
    Rel(user, salon, "Uses")
    Rel(salon, openai, "Uses", "Context-aware prompts and code summarization")
    Rel(salon, github, "Processes code from")
    Rel(user, api_to_test_code, "Interacts with")
    Rel(user, repo_to_text, "Interacts with")
    Rel(user, repo_to_c4, "Interacts with")
    Rel(api_to_test_code, openai, "Generates prompts and handles responses")
    Rel(repo_to_c4, openai, "Generates code summaries")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
