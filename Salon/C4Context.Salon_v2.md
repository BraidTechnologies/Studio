```mermaid
C4Context
  Person(user, "User", "Interacts with Salon tools for automated software development")

  System_Boundary(c1, "Salon") {
    System(salon, "Salon", "Technology demonstrator for automated software development tools using LLMs")
    Container(api_to_test_code, "api_to_test_code", "Python", "Generates Python test code from API specifications (JSON/YAML)")
    Container(repo_to_text, "repo_to_text", "Python", "Processes and analyzes codebases, consolidates source files")
    Container(repo_to_c4, "repo_to_c4.py", "Python", "Generates C4 architecture diagrams from GitHub repositories")
  }

  Rel(user, salon, "Uses", "CLI and API")
  Rel(api_to_test_code, salon, "Generates test code", "")
  Rel(repo_to_text, salon, "Analyzes code", "")
  Rel(repo_to_c4, salon, "Generates diagrams", "")

  System_Ext(openai, "OpenAI", "Provides LLM services")

  Rel(api_to_test_code, openai, "Uses", "API")
  Rel(repo_to_c4, openai, "Uses", "API for code summarization")
  Rel(repo_to_text, openai, "Uses", "API for code summarization")



```

footer "Generated by Salon from Braid Technologies, 28/02/2025"
