```mermaid
C4Context
  System_Boundary(c1, "Salon") {
    Container(api_to_test_code, "api_to_test_code", "Python", "Generates Python test code from API specifications (JSON/YAML). Uses OpenAI for context-aware prompts.")
    Container(repo_to_text, "repo_to_text", "Python", "Processes codebases, concatenates source files, enables LLM-based analysis. Adapted from NotebookLM.")
    Container(repo_to_c4, "repo_to_c4.py", "Python", "Generates C4 diagrams from GitHub repositories. Includes API integration for code summarization and file version management.")
    Container(repo_to_text_py, "repo_to_text.py", "Python", "Processes a local GitHub repository, concatenating file contents into text files.")

    Rel(api_to_test_code, repo_to_text, "Uses", "")
    Rel(repo_to_c4, repo_to_text, "Uses", "")


  }

  System_Ext(openai, "OpenAI", "Provides LLM functionality")
  Rel(api_to_test_code, openai, "Uses", "API")
  Rel(repo_to_c4, openai, "Uses", "API")
  Rel(repo_to_text, openai, "Uses", "API")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
