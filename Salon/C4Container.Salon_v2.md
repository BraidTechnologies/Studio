```mermaid
C4Context
  System_Boundary(c1, "Salon") {
    Container(api_to_test_code, "api_to_test_code", "Python", "Generates Python test code from API specifications (JSON/YAML). Uses OpenAI for context-aware prompts.")
    Container(repo_to_text, "repo_to_text", "Python", "Processes and analyzes codebases. Concatenates source files for LLM analysis.")
    Container(repo_to_c4, "repo_to_c4.py", "Python", "Generates C4 diagrams from GitHub repositories. Uses API for code summarization.")

  }
  System_Ext(OpenAI, "OpenAI", "Provides LLM API for code generation and summarization")

  Rel(api_to_test_code, OpenAI, "Uses", "API")
  Rel(repo_to_c4, OpenAI, "Uses", "API")

```

footer "Generated by Salon from Braid Technologies, 28/02/2025"
