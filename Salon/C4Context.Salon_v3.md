```mermaid
C4Context
  Person(user, "User", "Interacts with Salon tools for automated software development")

  System(salon, "Salon", "Technology demonstrator for automated software development using LLMs")

  Rel(user, salon, "Uses", "CLI and API")

  System_Ext(openai, "OpenAI", "Provides LLM services for code generation and analysis")

  Rel(salon, openai, "Uses", "API")

  System_Boundary(tools, "Salon Tools") {
    Container(api_to_test_code, "api_to_test_code", "Python", "Generates Python test code from API specifications (JSON/YAML)")
    Container(repo_to_text, "repo_to_text", "Python", "Processes and analyzes codebases, consolidating source code into text files")
    Container(repo_to_c4, "repo_to_c4", "Python", "Generates C4 architecture diagrams from GitHub repositories")
  }

  Rel(user, api_to_test_code, "Uses", "CLI")
  Rel(api_to_test_code, openai, "Uses", "API for code generation")

  Rel(user, repo_to_text, "Uses", "CLI")

  Rel(user, repo_to_c4, "Uses", "CLI")
  Rel(repo_to_c4, openai, "Uses", "API for code summarization")

```

%%% Generated by Salon from Braid Technologies, 28/02/2025
