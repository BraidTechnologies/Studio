```mermaid
C4Context
  Person(user, "User", "Uses Salon tools for software development")

  System_Boundary(c1, "Salon") {
    System(apitest, "ApiTest", "Generates Python test code from API specifications")
    System(repototext, "RepoToText", "Processes and analyzes codebases")
    System(repotoc4, "RepoToC4", "Generates C4 architecture diagrams from GitHub repositories")
  }

  Rel(user, apitest, "Uses", "Generates test code")
  Rel(user, repototext, "Uses", "Analyzes code")
  Rel(user, repotoc4, "Uses", "Generates diagrams")

  System_Ext(openai, "OpenAI", "Provides LLM for code analysis and test generation")

  Rel(apitest, openai, "Uses", "Code generation")
  Rel(repototext, openai, "Uses", "Code analysis")
  Rel(repotoc4, openai, "Uses", "Diagram generation")


```

%% Generated by Salon from Braid Technologies, 28/02/2025
