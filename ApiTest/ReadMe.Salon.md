**Directory test**

This codebase tests various API endpoints using `pytest` and `requests`.  Each module targets a specific endpoint like `/chunk`, `/classify`, `/embed`, `/findtheme`, and others.  Tests verify valid requests and responses, including schema validation with `jsonschema` where applicable, and error handling for invalid requests.  Common elements include using environment variables for the `BASE_URL` and `SessionKey`, constructing endpoint URLs, and checking response status codes (200 for success, 400 for bad requests).  Fixtures provide sample request data.  Tests cover various scenarios, from missing parameters to specialized requests like survey or code summarization.


Generated by Salon from Braid Technologies, 28/02/2025