```mermaid
    C4Component
    title Common Python Library

    System_Boundary(lib_boundary, Common Python Library) {
        Component(chunk_repo, "ChunkRepository", "Manages CRUD operations for chunks via the Braid API")
        Component(chunk_model, "IStoredChunk", "Data model for chunks", "Embeddings, Text renderings, Summaries, Related chunks, Metadata")
        Component(page_repo, "PageRepository", "Handles storage and retrieval of pages")
        Component(page_model, "IStoredPage", "Data model for pages", "Primarily storing HTML content")
        Component(storable_model, "IStorable", "Base class for storable entities")
        Component(query_spec, "IStorableQuerySpec", "Query parameters for data retrieval")
        Component(op_result, "IStorableOperationResult", "Operation result wrapper")
        Component(dict_util, "DictToObject", "Utility for JSON-to-object conversion")
    }

    Rel(chunk_repo, chunk_model, "uses")
    Rel(page_repo, page_model, "uses")
    Rel(chunk_model, storable_model, "inherits from")
    Rel(page_model, storable_model, "inherits from")
    Rel(query_spec, storable_model, "used as query spec")
    Rel(dict_util, chunk_model, "converts JSON to object")
    Rel(dict_util, page_model, "converts JSON to object")
    Rel(dict_util, storable_model, "converts JSON to object")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
