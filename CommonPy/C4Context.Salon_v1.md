```mermaid
C4Context
title Common Python Library Context Diagram

Person(user, "User")

System_Boundary(lib_boundary, "Common Python Library") {
    System(lib, "Python Library", "A Python library providing repository patterns and utilities for interacting with the Braid API.")
    
    System(chunk_repo, "Chunk Repository", "Manages CRUD operations for chunks via the Braid API")
    System(page_repo, "Page Repository", "Handles storage and retrieval of pages")
    System(storable, "Common Storable Types", "Base classes and interfaces for storable entities")
    System(type_utils, "Type Utilities", "Utilities for dictionary-to-object conversion and value casting")
}

System_Ext(api, "Braid API", "External API for data storage and retrieval")

Rel(user, lib, "Uses")
Rel(lib, chunk_repo, "Includes")
Rel(lib, page_repo, "Includes")
Rel(lib, storable, "Includes")
Rel(lib, type_utils, "Includes")
Rel(chunk_repo, api, "Interacts with Braid API")
Rel(page_repo, api, "Interacts with Braid API")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
