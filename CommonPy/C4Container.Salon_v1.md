```mermaid
    C4Container
    title Common Python Library for Braid API

    Person(dev, "Developer", "Works on integrating with the Braid API")

    System_Boundary(braid_lib, "Python Library") {
        Container(chunk_repo, "ChunkRepository", "Python Class", "Manages CRUD operations for chunks via the Braid API")
        Container(page_repo, "PageRepository", "Python Class", "Handles storage and retrieval of pages")
        Container(storable_types, "Storable Types", "Python Module", "Defines base classes for storable entities and query specs")
        Container(type_utils, "Type Utilities", "Python Module", "Provides utilities for dictionary-to-object conversion and safe casting")

        Container(chunk_api, "chunk_repository_api.py", "Python API", "API for managing data in the Chunk table of the Braid API") {
            Container(chunk_api_class, "ChunkRepository", "Python Class", "Manages CRUD operations for chunks")
        }
        Container(chunk_api_types, "chunk_repository_api_types.py", "Python Types", "Defines types for managing data related to embeddings and text renderings") {
            Container(chunk_type, "IStoredChunk", "Python Class", "Data model for chunks")
        }

        Container(page_api, "page_repository_api.py", "Python API", "API for managing data in the Page table of the Braid API") {
            Container(page_api_class, "PageRepository", "Python Class", "Handles storage and retrieval of pages")
        }
        Container(page_api_types, "page_repository_api_types.py", "Python Types", "Defines the data model for pages") {
            Container(page_type, "IStoredPage", "Python Class", "Data model for page storing HTML content")
        }
    }

    System_Ext(braid_api, "Braid API", "External API", "API for storing and managing chunks and pages of data")

    Rel(dev, braid_lib, "Uses")
    Rel(chunk_repo, braid_api, "Interacts with via HTTP API")
    Rel(page_repo, braid_api, "Interacts with via HTTP API")
    Rel(chunk_repo, storable_types, "Uses")
    Rel(page_repo, storable_types, "Uses")
    Rel(chunk_repo, type_utils, "Uses for data conversion")
    Rel(page_repo, type_utils, "Uses for data conversion")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
