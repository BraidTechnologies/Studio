```mermaid
C4Container
  System_Boundary(c1, "Python Library") {
    Container(chunk_repo, "Chunk Repository API", "Python", "Manages CRUD operations for chunks via the Braid API")
    Container(page_repo, "Page Repository API", "Python", "Handles storage and retrieval of pages via the Braid API")
    Container(common_types, "Common Types", "Python", "Data models and utility functions")
    Rel(chunk_repo, common_types, "Uses", "")
    Rel(page_repo, common_types, "Uses", "")

    System_Ext(braid_api, "Braid API", "REST API", "Provides access to chunk and page storage")
    Rel(chunk_repo, braid_api, "Interacts with", "HTTP")
    Rel(page_repo, braid_api, "Interacts with", "HTTP")

  }
```

%% Generated by Salon from Braid Technologies, 28/02/2025
