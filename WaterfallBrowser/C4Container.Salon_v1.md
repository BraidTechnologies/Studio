```mermaid
C4Container
title Waterfall Browser Container Diagram

Person(dev, "Developer", "Developer who works on Waterfall Browser")

System_Boundary(waterfall_browser, "Waterfall Browser") {
    Container(webapp, "Web Application", "React", "Allows users to navigate and view hierarchical data chunks", $tags="webapp")    
    Container_DB(database, "Data Storage", "Unknown Type", "Stores chunk data accessed by ChunkRetriever")   

    Container(app, "App", "React Component", "Main application component handling routing and layout")    
    Container(chunkRetriever, "ChunkRetriever", "React Component", "Manages the data fetching logic and state")    
    Container(chunkView, "ChunkView", "React Component", "Displays individual chunk data with navigation options") 
    Container(chunkViewLoading, "ChunkViewLoading", "React Component", "Loading state component")    
    Container(chunkViewError, "ChunkViewError", "React Component", "Error state component")
    Container(defusc, "Defusc", "TypeScript Module", "Base64 decoding utility for secure API key handling")
    Container(reportWebVitals, "reportWebVitals", "JavaScript Module", "Performance monitoring module using Web Vitals")
    Container(uiString, "UIString", "JavaScript Module", "Centralized UI text constants for internationalization support")
}

Rel(dev, webapp, "Develops and maintains")
Rel(dev, database, "Configures and manages data storage")

Rel(webapp, app, "Uses for routing and layout")
Rel(app, chunkRetriever, "Includes for data fetching")
Rel(chunkRetriever, chunkView, "Displays data")
Rel(chunkRetriever, chunkViewLoading, "Displays loading state")
Rel(chunkRetriever, chunkViewError, "Displays error state")
Rel(chunkRetriever, database, "Fetches data from")
Rel(chunkRetriever, defusc, "Uses for Base64 decoding")
Rel(webapp, reportWebVitals, "Monitors performance with")
Rel(app, uiString, "Uses for UI text constants")

System_Ext(external_service, "External API")
Rel(chunkRetriever, external_service, "Fetches chunk data from external service", "HTTP")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
