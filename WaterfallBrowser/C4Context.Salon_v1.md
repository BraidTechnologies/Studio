```mermaid
C4Context
    title Waterfall Browser - Context Diagram
    
    Person(user, "User", "Interacts with the Waterfall Browser app")
    
    System_Boundary(waterfallbrowser, "Waterfall Browser") {
        System(App, "App", "Main React application for navigating and viewing data chunks")
        System(Container, "ChunkRetriever", "Responsible for data fetching and management")
        System(Container, "ChunkView", "Displays individual chunk data with navigation options")
        System(Container, "ChunkViewLoading", "Displays loading state")
        System(Container, "ChunkViewError", "Displays error state")
        System(Container, "Defusc", "Base64 decoding utility")
        System(Container, "reportWebVitals", "Monitors performance metrics")
        System(Container, "UIString", "Contains UI text constants for internationalization")
    }
    
    System_Ext(ExternalApi, "External Data API", "Provides data chunks to the Waterfall Browser")
    
    Rel(user, App, "Uses")
    Rel(App, ChunkRetriever, "Initiates data fetching")
    Rel(ChunkRetriever, ChunkView, "Populates with chunk data on success")
    Rel(ChunkRetriever, ChunkViewLoading, "Displays for loading state")
    Rel(ChunkRetriever, ChunkViewError, "Displays for error state")
    Rel(ChunkRetriever, ExternalApi, "Fetches data chunks from")
    Rel(App, reportWebVitals, "Reports performance metrics to")
    Rel(App, UIString, "References for UI text")
    Rel(ChunkRetriever, Defusc, "Uses for API key decoding")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
