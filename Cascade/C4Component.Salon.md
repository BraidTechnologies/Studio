```mermaid
C4Component
title Cascade Chrome Extension - Component Diagram

System_Boundary(ext, "External Systems") {
    System_Ext(braid, "Braid API", "braid-api.azurewebsites.net", "Session validation")
    System_Ext(summarization_api, "Summarization API", "", "Text summarization")
    System_Ext(classification_api, "Classification API", "", "Content classification")
}

System_Boundary(chrome_extension, "Cascade Chrome Extension") {
    Component(popup, "Popup Interface", "popup.js", "Manages user authentication, displays results, communicates with Braid API")
    Component(content_script, "Content Script", "content.ts", "Handles web scraping, text extraction, and API communication")

    Rel(popup, braid, "Validates session", "JSON over HTTPS")
    Rel(popup, content_script, "Triggers scraping & displays results", "Chrome Extension Messaging")
    Rel(content_script, summarization_api, "Summarizes text", "JSON over HTTPS")
    Rel(content_script, classification_api, "Classifies content", "JSON over HTTPS")
}

Person(user, "User", "Interacts with the extension")
Rel(user, popup, "Interacts with", "UI interaction")


```

%% Generated by Salon from Braid Technologies, 28/02/2025
