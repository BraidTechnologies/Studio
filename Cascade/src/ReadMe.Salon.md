**content.ts**

This TypeScript module, `Cascade/src/content.ts`, serves as a content script for a Chrome extension that performs web scraping, text summarization, and content classification. 

### Global Variables
- `haveStartedScrape`: A Boolean flag preventing concurrent scraping operations.

### Key Functions
1. `suppressUnhandledPromiseRejection(event)`: Handles unhandled promise rejections, resets scraping state, and sends error messages to the background script.
2. `startScrape(key)`: Orchestrates the scraping, summarization, and classification process, enforces text length limits, and manages progressive feedback.

### Message Handling
- Listens for "Key" type messages to initiate scraping and prevents concurrent operations.

### API Integration
Communicates with external APIs for text summarization and classification.

### Dependencies
- Uses Artoo.js for scraping, Axios for HTTP requests, and Chrome Extension APIs for message passing.

Generated by Salon from Braid Technologies, 23/02/2025