```mermaid
C4Context
title Cascade Chrome Extension

Person(user, "User", "Interacts with the Chrome extension for web scraping, summarization, and classification.")

System(extension, "Cascade Chrome Extension", "Chrome extension for web scraping, text summarization, and content classification.")

System_Ext(braid_api, "Braid API", "Session validation and authentication service (braid-api.azurewebsites.net)")

System_Ext(summarization_api, "Summarization API", "External service for text summarization.")

System_Ext(classification_api, "Classification API", "External service for content classification.")

Rel(user, extension, "Interacts with", "UI")
Rel(extension, braid_api, "Authenticates with", "HTTPS")
Rel(extension, summarization_api, "Uses", "HTTPS")
Rel(extension, classification_api, "Uses", "HTTPS")

System_Boundary(extension_boundary, "Cascade Chrome Extension") {
  Container(content_script, "Content Script", "Handles web scraping, text extraction, and communication with external APIs.", "TypeScript, artoo.js, axios")
  Container(popup_interface, "Popup Interface", "Manages user authentication, input handling, and displays results.", "JavaScript")

  Rel(user, popup_interface, "Interacts with", "UI")
  Rel(popup_interface, content_script, "Sends messages to", "Chrome Extension Messaging")
  Rel(content_script, braid_api, "Authenticates with", "HTTPS")
  Rel(content_script, summarization_api, "Uses", "HTTPS")
  Rel(content_script, classification_api, "Uses", "HTTPS")
}
```

%% Generated by Salon from Braid Technologies, 28/02/2025
