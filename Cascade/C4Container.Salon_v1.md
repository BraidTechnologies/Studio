```mermaid
C4Container
title Cascade Chrome Extension

Person(user, "User") 

System_Boundary(cascade_boundary, "Cascade Extension") {
    Container(content_script, "Content Script", "TypeScript", "Handles web scraping operations and communicates with external APIs for processing text summarization and classification.")
    Container(popup_interface, "Popup Interface", "JavaScript", "Manages user authentication, displays processing results and feedback.")
    ContainerDb(storage, "Session Storage", "Chrome Storage", "Stores session keys and temporary data.")

    System_Ext(braid_api, "Braid API", "Validates session keys")
    System_Ext(external_apis, "External APIs", "Handles text summarization and classification")
}

Rel(user, popup_interface, "Interacts with")
Rel(popup_interface, braid_api, "Sends session key for validation")
Rel(popup_interface, content_script, "Sends message")
Rel(content_script, external_apis, "Communicates with")
Rel(content_script, storage, "Stores and retrieves session keys")
Rel(content_script, popup_interface, "Sends processing results")

```

%% Generated by Salon from Braid Technologies, 09/03/2025
