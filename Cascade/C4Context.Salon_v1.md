```mermaid
C4Context
title Cascade Chrome Extension

Person(user, "User", "Person using the Chrome extension")

System_Boundary(Extension_Boundary, "Cascade Chrome Extension") {
    Container(content_script, "Content Script", "TypeScript", "Handles web scraping, text extraction, and communicates with external APIs")
    Container(popup_interface, "Popup Interface", "JavaScript", "Manages user authentication and displays feedback")
}

System_Ext(braid_api, "Braid API", "Azure API", "Handles session validation")
System_Ext(external_apis, "External APIs", "Various APIs", "Provides text summarization and classification services")

Rel(user, popup_interface, "Enters session key and views processing results")
Rel(popup_interface, braid_api, "Sends session key for validation")
Rel(braid_api, popup_interface, "Returns validation result")
Rel(popup_interface, content_script, "Sends validation messages")
Rel(content_script, external_apis, "Communicates for text summarization and classification")
Rel(content_script, user, "Provides real-time visual feedback during operations")
```

%% Generated by Salon from Braid Technologies, 09/03/2025
