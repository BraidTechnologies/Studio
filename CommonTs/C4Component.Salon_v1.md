C4Context
  System_Boundary(c1, "Client") {
    Person(user, "User", "Interacts with the application")
    System(browser, "Browser", "Frontend application")
    System(mocha, "Mocha Test", "Runs tests against shared components")
  }

  System_Boundary(c2, "Server") {
    System(api, "API", "Backend application")
  }

  System_Boundary(c3, "Shared Components") {
    Rel(browser, api, "Uses", "API calls")
    Rel(mocha, api, "Tests", "API calls")
    Rel(api, shared_components, "Uses", "Shared components")
    Rel(browser, shared_components, "Uses", "Shared components")
    Rel(mocha, shared_components, "Uses", "Shared components")


    System_Boundary(api_classes, "API Classes") {
      Component(api_base, "Api", "Base class for API interactions")
      Component(activity_repo_api, "ActivityRepositoryApi", "Manages activity records")
      Component(chunk_repo_api, "ChunkRepositoryApi", "Handles text chunk operations")
      Component(find_enriched_chunk_api, "FindEnrichedChunkApi", "Finds and manages enriched text chunks")
      Component(query_model_api, "QueryModelApi", "Interfaces with AI models")
      Component(session_api, "SessionApi", "Manages user sessions")
      Component(storable_repo_api, "StorableRepositoryApi", "Base repository operations")

      Rel(activity_repo_api, api_base, "Extends", "")
      Rel(chunk_repo_api, api_base, "Extends", "")
      Rel(find_enriched_chunk_api, api_base, "Extends", "")
      Rel(query_model_api, api_base, "Extends", "")
      Rel(session_api, api_base, "Extends", "")

    }

    System_Boundary(data_models, "Data Models & Types") {
      Component(istorable, "IStorable", "Interface for storable objects")
      Component(imodel, "IModel", "Interface defining AI model capabilities")
      Component(enriched_chunk, "EnrichedChunk", "Enhanced chunk data structures")
      Component(ienvironment, "IEnvironment", "Environment configuration interface")

    }

    System_Boundary(utilities, "Utilities") {
      Component(asserts, "Asserts", "Validation utilities")
      Component(compress, "Compress", "String compression/decompression")
      Component(logging, "Logging", "Standardized logging functions")
      Component(errors, "Errors", "Custom error types")

    }

    System_Boundary(environment_management, "Environment Management") {
      Component(dev_env, "DevelopmentEnvironment", "")
      Component(staging_env, "StagingEnvironment", "")
      Component(prod_env, "ProductionEnvironment", "")
    }

    Rel(api_classes, data_models, "Uses", "")
    Rel(api_classes, utilities, "Uses", "")
    Rel(api_classes, environment_management, "Uses", "")


  }
  System_Ext(salon, "Salon", "Used for test code generation")
  Rel(api, salon, "Used by", "")

%% Generated by Salon from Braid Technologies, 28/02/2025
