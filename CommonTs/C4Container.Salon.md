```mermaid
C4Container

System_Boundary(c1, "Client") {
  Container(browser, "Browser", "TypeScript", "Renders UI and interacts with APIs")
  Container(mocha, "Mocha Test", "TypeScript", "Runs unit and integration tests")
}

System_Boundary(c2, "Server") {
  Container(api, "API", "TypeScript", "Provides backend services")
    ContainerDb(db, "Database", "Stores data", "MongoDB or similar")
}

System_Ext(salon, "Salon", "Code Generation Tool")
System_Ext(linkedin, "LinkedIn", "Authentication Provider")
System_Ext(fluid_relay, "Azure Fluid Relay", "Real-time Collaboration Service")
System_Ext(openai, "Azure OpenAI", "AI Model Provider")


Rel(browser, api, "Interacts with", "HTTPS")
Rel(mocha, api, "Tests", "HTTPS")
Rel(api, db, "Reads/Writes", "Database connection")
Rel(salon, api, "Generates test code for", "")
Rel(browser, linkedin, "Authenticates with", "OAuth 2.0")
Rel(api, linkedin, "Authenticates with", "OAuth 2.0")
Rel(api, fluid_relay, "Uses", "HTTPS")
Rel(api, openai, "Uses", "HTTPS")


```

%% Generated by Salon from Braid Technologies, 28/02/2025
