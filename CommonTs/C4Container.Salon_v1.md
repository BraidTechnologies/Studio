C4Context
  System_Ext(user, "User", "Interacts with the Braid application")
  System_Boundary(c1, "Braid Application") {
    Container(browser, "Browser", "TypeScript", "Frontend UI")
    Container(api, "API", "TypeScript", "Backend API")
    ContainerDb(db, "Persistent Storage", "Stores data objects")
    Container(tests, "Mocha Tests", "TypeScript", "Frontend Testing")
    Rel(user, browser, "Interacts with", "HTTPS")
    Rel(browser, api, "Uses", "HTTPS")
    Rel(api, db, "Reads/Writes", "")
    Rel(tests, browser, "Tests", "")
    Container(shared, "Shared Components", "TypeScript", "Common code")
    Rel(browser, shared, "Uses", "")
    Rel(api, shared, "Uses", "")
    Rel(tests, shared, "Uses", "")

    System_Boundary(c2, "Shared Components - API") {
      Container(api_base, "Api", "Handles environment and session management")
      Container(activity_repo_api, "ActivityRepositoryApi", "Manages activity records")
      Container(chunk_repo_api, "ChunkRepositoryApi", "Handles text chunk operations")
      Container(find_enriched_chunk_api, "FindEnrichedChunkApi", "Finds and manages enriched text chunks")
      Container(query_model_api, "QueryModelApi", "Interfaces with AI models")
      Container(session_api, "SessionApi", "Manages user sessions")
      Container(storable_repo_api, "StorableRepositoryApi", "Base repository operations")
      Container(page_repo_api, "PageRepositoryApi", "Manages page storage and retrieval")
      Rel(activity_repo_api, api_base, "Extends", "")
      Rel(chunk_repo_api, api_base, "Extends", "")
      Rel(find_enriched_chunk_api, api_base, "Extends", "")
      Rel(query_model_api, api_base, "Extends", "")
      Rel(session_api, api_base, "Extends", "")
      Rel(page_repo_api, api_base, "Extends", "")
    }

    System_Boundary(c3, "Shared Components - Utilities & Models") {
        Container(asserts, "Asserts", "Validation utilities")
        Container(compress, "Compress", "String compression/decompression")
        Container(logging, "Logging", "Standardized logging")
        Container(errors, "Errors", "Custom error types")
        Container(environment, "Environment Management", "Handles environment configurations")
        Container(models, "Data Models & Types", "Defines data structures and interfaces")

    }

    System_Ext(linkedin, "LinkedIn API", "Used for authentication")
    Rel(api, linkedin, "Uses", "HTTPS")

    System_Ext(azure_openai, "Azure OpenAI", "Provides AI models and embeddings")
    Rel(api, azure_openai, "Uses", "HTTPS")
    Rel(query_model_api, azure_openai, "Uses", "HTTPS")


    System_Ext(fluid_relay, "Azure Fluid Relay", "Provides real-time collaboration services")
    Rel(api, fluid_relay, "Uses", "HTTPS")
  }

%% Generated by Salon from Braid Technologies, 28/02/2025
