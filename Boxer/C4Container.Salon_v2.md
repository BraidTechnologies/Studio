```mermaid
C4Container
  System_Boundary(c1, "Boxer Application") {
    Container(web_app, "Web Application", "Typescript/React", "Provides user interface and client-side logic")
    Container(fluid_service, "Fluid Service", "Fluid Framework", "Handles real-time collaboration and data synchronization")
    Container(ai_service, "AI Service", "Large Language Model (LLM)", "Provides AI capabilities and processes LLM requests")
    Container(activity_repository, "Activity Repository", "Cosmos DB", "Stores user activities and message history")

    Rel(web_app, fluid_service, "Syncs data", "WebSocket")
    Rel(web_app, ai_service, "Sends requests", "API")
    Rel(web_app, activity_repository, "Saves and loads activity records", "API")
    Rel(fluid_service, activity_repository, "Persists shared data", "API")
  }

  System_Ext(user, "User", "Interacts with the chat application")

  Rel(user, web_app, "Interacts", "HTTPS")
```

%% Generated by Salon from Braid Technologies, 28/02/2025
