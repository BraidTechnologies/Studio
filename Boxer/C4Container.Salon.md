```mermaid
C4Container

System_Boundary(c1, "Boxer Chat Application") {

  Container(client, "Web Client", "TypeScript/React", "Provides real-time chat interface")
  Container(fluid, "Fluid Framework Service", "Handles real-time collaboration and data synchronization")
  Container(api, "Braid Backend API", "", "Provides API endpoints for key retrieval, summarization, and AI interaction")
  Container(ai, "LLM Service", "", "Provides Large Language Model capabilities")
  Container(db, "Activity Repository (Cosmos DB)", "", "Stores user activities and message history")

}

System_Ext(user, "User", "Interacts with the chat application")

Rel(user, client, "Interacts with", "HTTPS")
Rel(client, fluid, "Collaborates via", "")
Rel(client, api, "Uses", "HTTPS")
Rel(api, ai, "Interacts with", "HTTPS/API")
Rel(api, db, "Reads/Writes", "")

```

%% Generated by Salon from Braid Technologies, 28/02/2025
