C4Context
  Person(user, "User", "Interacts with the Boxer application")

  System_Boundary(c1, "Boxer Application") {
    Container(ui, "UI", "TypeScript/React", "Provides the user interface for real-time chat and AI interaction")
    Container(core, "Core", "TypeScript", "Contains the core business logic, data models, and services")
    ContainerDb(activity_repository, "Activity Repository", "Cosmos DB", "Stores user activities and message history")
    Container(fluid_framework, "Fluid Framework", "", "Handles real-time collaboration and data synchronization")

    Rel(user, ui, "Interacts with", "HTTPS")
    Rel(ui, core, "Uses", "")
    Rel(core, activity_repository, "Reads/Writes", "")
    Rel(core, fluid_framework, "Uses", "")
    Container(ai_connection, "AI Connection", "", "Manages interactions with the LLM (Large Language Model)")

    Rel(core, ai_connection, "Uses", "API calls")
  }

  System_Ext(llm, "Large Language Model (LLM)", "", "Provides AI capabilities for the chat application")

  Rel(ai_connection, llm, "Interacts with", "API calls")

%% Generated by Salon from Braid Technologies, 28/02/2025
